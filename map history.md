# map-history.md

Riwayat lengkap tiap sesi kerja di repo ini, urut TANGGAL (bukan urut section kayak `map.md`). Ini adalah `map.md`'s bagian "## Status" yang dulu, dipindah ke file sendiri 24 Agustus 2026 biar `map.md` tetap pendek/murah dibaca ulang tiap sesi baru.

Baca file ini kalau: mau tau KENAPA suatu bagian kode kayak sekarang, detail bug spesifik yang pernah ketemu dan gimana fixnya, atau jejak keputusan arsitektur. Kalau cuma butuh tau KONDISI SEKARANG (halaman mana yang udah migrasi, struktur target, checklist), itu ada di `map.md` — gak perlu baca file ini.

---

## Status

Update file ini (append di bagian bawah) tiap kali ada sesi kerja baru yang selesai — dan coret/update daftar bagian 4 di `map.md` juga kalau ada halaman yang selesai dimigrasi, jangan biarin `map.md` basi.

**Update 22 Agustus 2026 (sesi kedua):** `kompres-pdf.html` sudah dimigrasi ke `/app`. Catatan buat sesi berikutnya yang lanjutin ke `export-gambar-gabungan.html` (urutan #2):
- Pola yang dipakai: `mount(container)` di-scope pakai `container.querySelector`/`container.querySelectorAll` (bukan `document.*`), CSS lama di-copy apa adanya ke `style.css` per halaman lalu di-`<link>`-kan sekali lewat `new URL('./style.css', import.meta.url)`, HTML lama disimpan sebagai template string di `markup.js` (zero-build, gak ada cara `import` file `.html` langsung).
- Script vendor pihak ketiga (CDN) di-load on-demand di dalam `mount()`, dicek dulu `window.<Global>` biar gak double-load kalau halaman di-mount ulang.
- Halaman ini gak ada listener di level `document`/`window`, jadi `unmount()`-nya nyaris kosong — halaman lain (terutama yang pakai drag globally atau `PNMAuth`) kemungkinan butuh `unmount()` yang beneran bersih-bersih (`removeEventListener`, clear interval, dsb). Jangan asumsikan pola kosong ini berlaku umum.
- `state/store.js` di bagian 3 BELUM dibuat — belum ada halaman yang butuh dia (kompres-pdf gak pakai Supabase/auth sama sekali). Baru relevan mulai dari `export-gambar-gabungan.html` dan seterusnya, yang mulai nyentuh `PNMAuth`.
- Diverifikasi pakai Playwright headless (bukan cuma baca kode): mount awal, ganti tab, ganti route via hash (unmount → remount), dan fallback halaman yang belum dimigrasi — semua nol error konsol.

**Update 22 Agustus 2026 (sesi keempat):** `konversian.html` sudah dimigrasi ke `/app/pages/konversian/` (urutan #3 selesai). Ini beda pendekatan dari 2 halaman sebelumnya, jadi dicatat detail:
- **Pendekatan beda dari export-gambar/kompres-pdf, SENGAJA**: file ini TIDAK di-rescope dari `document.getElementById(...)` ke `container.querySelector(...)` di ribuan titik pemanggilan — terlalu berisiko buat file sebesar 6998+1351 baris tanpa test browser end-to-end. Ini tetap aman karena router.js menjamin cuma SATU halaman termount di satu waktu (unmount() + `container.innerHTML=''` sebelum halaman baru di-mount), dan konversian.js/sph-module.js menangkap semua referensi elemen SETELAH markup di-inject — jadi `document.getElementById` tetap nemu elemen yang benar.
- **Body asli TIDAK diubah**, kecuali 5 patch bedah kecil (semua ditandai komentar "SPA migration" di titik masing-masing) buat nangkep handle yang dibutuhin `unmount()`: (1) return value `PNMAuth.onAuthStateChange()` ditangkep ke `__konvAuthUnsub`; (2) `setInterval` heartbeat badge sesi (30 detik) ditangkep ke `__konvHeartbeatTimer`; (3) listener `document.addEventListener('click', ...)` buat nutup autocomplete di-nama-in biar bisa di-`removeEventListener`; (4) baris `window.__konvBridge = {...}` ditambahin di paling akhir eksekusi konversian.js buat ngumpulin semua handle di atas; (5) inline script "conv-flow-ux" (dulu `<script>` ke-2 di HTML) ditambahin `window.convFlow._cleanup()` — disconnect 2 `MutationObserver` + lepas keydown listener + clear 2 timer, sebelumnya cuma expose showSuccess/showError/reset tanpa cara bersih-bersih dari luar.
- **2 bug path relatif yang KETEMU dan DIPERBAIKI** akibat pindah dari `/konversian.html` ke `/app/shell.html#konversian`: (a) `navigateToEditProduk()` pakai `new URL('crud-produk.html', window.location.href)` — kalau dibiarkan bakal resolve ke `/app/crud-produk.html` (gak ada), sekarang absolute `/crud-produk.html` (nembak file standalone yang masih ada, karena crud-produk.html BELUM dimigrasi — lihat urutan #4); (b) sph-module.js pakai path `assets/HEADER_SAMAYA.png` dkk (relatif) buat aset kop surat SPH, sekarang absolute `/assets/...`.
- **1 bug hash-stripping yang KETEMU dan DIPERBAIKI**: `restoreNavContext()` (dipanggil kalau ada `?resume=1`, alur balik dari edit produk di crud-produk.html) manggil `window.history.replaceState({}, '', window.location.pathname)` buat bersihin query string — di halaman standalone ini aman, tapi di SPA bakal ikut ngilangin `#konversian` dari address bar (reload berikutnya jatuh ke `DEFAULT_ROUTE` router.js, bukan balik ke konversian). Sekarang `window.location.hash` dipertahankan.
- **Tombol "Kembali ke Konversi" di `crud-produk.js` (baris ~178) DIUPDATE juga** (di luar rencana awal, ketemu pas testing manual) — sebelumnya redirect ke `konversian.html?resume=1&...` (halaman lama standalone), sekarang ke `/app/shell.html?resume=1&refreshed=...&sesi=...#konversian` (query string di LUAR hash — `restoreNavContext()` baca `window.location.search`, bukan query yang nempel di dalam hash). Ini gak nunggu crud-produk.html ikut dimigrasi ke SPA (urutan #4) — crud-produk.html TETAP standalone kayak sebelumnya, cuma tombol baliknya aja yang sekarang nunjuk ke rute SPA yang udah ada. Kalau nanti crud-produk.html ikut dimigrasi, baris ini kemungkinan gak perlu diubah lagi (udah nunjuk ke tujuan yang benar).
- **2 modal yang di-inject ke `document.body` langsung (bukan `container`)** ditemukan: `setupSphRiwayatModal()` dan `setupKonversiRiwayatModal()`, dua-duanya IIFE top-level yang jalan tiap kali `mount()` dipanggil. Tanpa guard, remount bakal numpuk elemen ber-id sama di body. Dikasih guard (`document.getElementById(id)?.remove()` sebelum bikin baru) DAN dibersihin lagi di `unmount()`.
- **Vendor scripts**: exceljs/pdfjs-dist/tesseract.js/jspdf/docx/`shared/toast.js` independen (Promise.all, pola sama kayak kompres-pdf); `@supabase/supabase-js` CDN → `shared/supabase-client.js` → `shared/auth-session.js` tetap sequential (pola sama kayak export-gambar, alasan sama: dependency lewat global).
- **CSS**: `pnm-universal.css` (shared antar halaman dashboard/stok/crud-produk) sekarang di-link dari `ensureStyle()` di `index.js` dengan id tetap (`shared-pnm-universal-css`) biar halaman lain yang migrasi belakangan bisa skip kalau udah ke-load — sama pola dengan Google Fonts + Phosphor + Tabler icon links (semua di-load sekali, id-checked). `pnm-konversian-rebase.css` (page-specific) tetap sendiri di `style.css` folder ini, gak berubah isinya.
- **BELUM dikerjakan** (baca bagian 2 buat batas section): konversian.js (6998 baris, sekarang di dalam `index.js`) MASIH SATU FILE — belum dipecah jadi `search.js`/`clipboard.js`/`sph.js`/`dictionary.js`/`set-mendekati.js`/`permintaan-rs.js`/`realtime.js` kayak target struktur bagian 3. Yang dikerjain sesi ini murni "masuk SPA jalan dulu, gak regresi (dan gak nambah bug path/lifecycle baru)" — pemecahan modul granular itu langkah refactor terpisah, resikonya beda (motong 22 section yang saling manggil fungsi, bukan soal auth/lifecycle lagi).
- **BELUM diverifikasi pakai Playwright** — beda dari 2 sesi sebelumnya. Sandbox investigasi sesi ini gak punya akses network ke domain CDN (`jsdelivr.net`, `unpkg.com`) ATAUPUN ke project Supabase (`ptkkbsemihcyndisjoor.supabase.co`), jadi gak ada cara jalanin browser asli terhadap kode ini dari sini. Yang SUDAH dicek: `node --check` (syntax valid) di `index.js`/`markup.js`/`router.js`, audit manual tiap `setInterval`/`addEventListener(window|document)`/`MutationObserver`/realtime-channel/modal-body-inject buat mastiin `unmount()` lengkap, dan grep menyeluruh buat path relatif yang bisa nyasar gara-gara pindah base URL dari `/konversian.html` ke `/app/shell.html`. **Sesi berikutnya atau kamu sendiri (yang udah `serve` lokal) WAJIB test manual/Playwright dulu sebelum ini dianggap selesai** — terutama: login gate, buka/lanjut sesi konversi + realtime kolaborasi (2 tab), Export Excel + Simpan ke Drive (modal conv-flow-ux), Generate SPH, dan mount→unmount→remount (pindah ke kompres-pdf/export-gambar lalu balik lagi ke `#konversian`) buat mastiin gak ada listener/timer/modal yang numpuk.

**Update 23 Agustus 2026 (post-testing, masih sesi keempat):** Ketemu pas user testing manual — `index.html` (dashboard/beranda, BELUM dimigrasi) punya link hardcoded ke halaman legacy buat **SEMUA TIGA halaman yang udah dimigrasi**, bukan cuma konversian: pill selector + `moduleFaces.*.url` (config buat cube 3D) di baris ~664-880-an. Ini kelewat di 2 sesi migrasi sebelumnya juga (kemungkinan karena diverifikasi langsung ke URL SPA-nya, bukan lewat klik dari dashboard). Semua 3 diganti ke rute SPA:
  - `#pill-konversian` + `moduleFaces.front.url`: `/konversian.html` → `/app/shell.html#konversian`
  - `#pill-kompres-pdf` + `moduleFaces.top.url`: `/kompres-pdf.html` → `/app/shell.html#kompres-pdf`
  - `#pill-export-gambar` + `moduleFaces.bottom.url`: `/export-gambar-gabungan.html` → `/app/shell.html#export-gambar`
  - `#pill-stok`/`#pill-crud` (`/stok.html`, `/crud-produk.html`) SENGAJA dibiarkan — dua itu memang belum dimigrasi.
**Checklist buat sesi migrasi berikutnya (crud-produk.html, urutan #4): jangan lupa update index.html juga**, bukan cuma bikin folder halaman barunya doang — bug ini gampang kelewat karena SPA-nya sendiri tetap "keliatan jalan" kalau dites langsung dari URL-nya, cuma jalur masuk dari dashboard yang diam-diam masih ke legacy.

**Update 23 Agustus 2026 (sesi kelima):** Eksekusi `visi-pionir-workspaces.md` bagian 5 langkah 1 — `index.html` (gerbang) di-port ke `app/pages/home/` (`index.js`/`markup.js`/`style.css`, pola sama seperti 3 tools lain), dan route `'home'` ditambahin ke `router.js` (`DEFAULT_ROUTE` BELUM diubah — masih `'kompres-pdf'`, sesuai urutan: `#home` harus kebukti jalan dulu sebelum root `index.html` ditimpa jadi shell di langkah 2). Link tiap modul dibiarkan APA ADANYA (masih `/app/shell.html#...` untuk yang migrated, masih `/nama.html` untuk yang legacy — belum hash-nav).

**DI LUAR rencana port verbatim di atas** — permintaan eksplisit user: kubus 3D (Rubik cube drag/rotate + `moduleFaces` + `requestAnimationFrame` loop), aurora background blobs, dan cursor-glow follower SEMUA DIBUANG, diganti grid kartu modul flat (`.module-card`, tanpa blur/glass/gradient). Konsekuensinya `app/pages/home/index.js` gak punya listener `window`/`document` sama sekali (gak ada lagi drag-pointer state buat di-`unmount()`) — `unmount()`-nya jadi sesederhana kompres-pdf punya. Root `index.html` (file terpisah, MASIH standalone, belum jadi shell) JUGA ditulis ulang dengan desain flat yang sama persis (isinya sengaja mirror `app/pages/home/`, dua-duanya masih hidup terpisah sampai langkah 2 visi tereksekusi) — supaya user yang buka `index.html` langsung juga langsung lihat hasilnya, gak nunggu 4 langkah sisanya kelar dulu.

Diverifikasi: `node --check` (syntax valid) di `markup.js`/`index.js`/`router.js`, dan `python -m http.server` lokal — semua file baru (`index.html`, `app/pages/home/*`, `app/shell.html`) ke-serve 200. **Belum diverifikasi pakai browser asli** (sandbox investigasi ini gak selalu punya akses ke `fonts.googleapis.com` — cuma dicek path/serve, bukan render visual). Sesi berikutnya/kamu sendiri yang `serve` lokal: cek `/#home` dan `/index.html` langsung di browser (toggle tema, hover/klik tiap kartu modul) sebelum lanjut ke langkah 2 (`index.html` root jadi shell + `router.js` `DEFAULT_ROUTE`).

**Update 23 Agustus 2026 (sesi keenam, bugfix hasil testing user):** User test manual di sandbox lokal (`#home` via Chrome) nemu 2 bug:
1. **Klik "Beranda" di `konversian` nyasar ke `index.html` lama** — `app/pages/konversian/markup.js` masih hardcode `href="/index.html"` (belum tau `#home` ada, karena ditulis sebelum sesi kelima). Diganti ke `href="/app/shell.html#home"`.
2. **"Balik pakai tombol Back di Chrome bikin tampilan `#home` jadi 'scale poorly'"** — root cause: `pnm-universal.css` (shared, dimuat sekali oleh konversian/lainnya dan TIDAK PERNAH di-unload — lihat bagian 1) berisi selector bentrok dengan classname yang dipakai `app/pages/home/style.css`: `.stat-val` (bentrok persis, spesifisitas sama → siapa yang paling akhir di-load ke `<head>` yang menang, dan `pnm-universal.css` selalu telat dimuat dibanding home) dan bare `header{background:...;gap:...;z-index:...;flex-shrink:...}` (elemen selector — kalah spesifisitas buat properti yang `.home-header` sudah override, tapi menang buat 4 properti yang tadinya TIDAK di-override). Balik dari tool lain (yang sempat mount pnm-universal.css) ke `#home` via Back = persis skenario ini. Fix: classname yang bentrok di-rename jadi prefixed (`stat-item`→`home-stat-item`, `stat-val`→`home-stat-val`, `stat-lbl`→`home-stat-lbl`, `header-right`→`home-header-right`) di `app/pages/home/markup.js`+`style.css` DAN di root `index.html` (biar konsisten, walau index.html standalone gak pernah load pnm-universal.css jadi gak collision beneran — cuma jaga-jaga kalau nanti disatuin), plus `.home-header` dikasih reset eksplisit (`background:transparent;gap:0;flex-shrink:initial;z-index:auto`) buat nutup celah 4 properti yang gak ke-cover classname manapun.
**Pelajaran buat halaman SPA baru ke depannya:** `pnm-universal.css` itu ~2231 baris dipakai lintas modul dan TIDAK PERNAH di-unload sepanjang sesi SPA — classname apapun yang dipakai halaman baru WAJIB di-grep dulu terhadap file itu sebelum dianggap aman, bukan cuma diasumsikan "gak bakal ketemu nama yang sama". Halaman yang sengaja gak mau ikutan tema `pnm-universal.css` (seperti `home`, karena desainnya flat/independen) risikonya lebih tinggi dari yang sengaja MEMANG pakai `pnm-universal.css` (seperti konversian) — karena yang terakhir emang didesain buat cocok sama stylesheet itu.

**Update 22 Agustus 2026 (sesi ketiga):** `export-gambar-gabungan.html` sudah dimigrasi. Catatan tambahan buat sesi berikutnya (lanjut ke `konversian.html`, urutan #3):
- Halaman ini TERNYATA butuh `unmount()` yang beneran bersih-bersih (beda dari kompres-pdf yang nyaris kosong) — ada 2 hal yang harus di-cleanup: (1) `IntersectionObserver` yang di-`disconnect()`, (2) unsubscribe function dari `PNMAuth.onAuthStateChange()` yang harus dipanggil. Keduanya disimpan di variabel module-scope (`activeObserver`, `authUnsubscribe`), bukan di dalam `mount()`, biar `unmount()` bisa akses.
- Markup lama pakai inline `onclick="..."`/`oninput="..."` yang manggil fungsi lewat global scope (`applyBulkInput`, `renderFileList`, `mergeAndPreview`, `exportCanvasAsPNG`, `toggleFile`). Karena modul sekarang `type="module"` (scope sendiri, gak otomatis nempel ke `window`), fungsi-fungsi itu harus di-expose manual ke `window.*` di akhir `mount()` DAN di-`delete`-in di `unmount()` — kalau lupa langkah kedua, mereka bakal nyangkut dan bisa nabrak halaman lain yang kebetulan pakai nama fungsi sama.
- Vendor script untuk halaman ini (`@supabase/supabase-js` CDN → `/shared/supabase-client.js` → `/shared/auth-session.js`) **harus dimuat berurutan**, gak bisa `Promise.all` kayak punya kompres-pdf — tiap file bergantung ke global yang dibikin file sebelumnya (`window.supabase` → `window.pnmSupabase` → `window.PNMAuth`).
- Ada temuan baru yang gak kesebut sebelumnya: halaman ini punya sistem toast KETIGA (bukan cuma 2 skema yang disebut di bagian 1) — `toast()` lokal + `#toastRoot`, gak pakai `shared/toast.js`/`PNMToast` sama sekali, dan CSS-nya nempel ke tema gelap "glass" khusus halaman ini. Dibiarkan apa adanya buat migrasi ini — nyatuin ke `PNMToast` itu keputusan terpisah (kemungkinan gak worth it kalau visualnya beda jauh), bukan bagian wajib checklist bagian 5.
- Diverifikasi end-to-end (bukan cuma smoke test kayak kompres-pdf): pakai `shared/supabase-client.js` dan `shared/auth-session.js` yang ASLI (file sungguhan, bukan stub) — cuma SDK Supabase-nya (CDN jsdelivr) yang di-mock via Playwright route interception, karena sandbox investigasi ini blokir domain itu. Alur login-salah → login-benar → lolos-whitelist → file list ke-load → toggle-file semuanya jalan lewat kode asli, bukan diasumsikan dari baca kode doang.
**Update 23 Agustus 2026 (sesi kesembilan):** Dua fix + satu cleanup atas laporan user:
1. **Bug "tema #home kacau + blink aneh" pas balik dari `stok.html`/`#export-gambar`, DIPERBAIKI** — root cause BUKAN di `app/pages/home/` (yang sudah difix sesi keenam), tapi di `konversian/index.js`: `pnm-universal.css` di-treat sebagai "SHARED, nempel selamanya" (di-load sekali via `ensureStyle()`, id `shared-pnm-universal-css`, TIDAK PERNAH dilepas walau `unmount()` dipanggil). Begitu user pernah mampir ke `#konversian` dalam satu sesi SPA, file itu nempel di `<head>` seterusnya dan bocor selector bare/unscoped-nya (`header{}`, `.stat-val`, dst — sama kelas bug dengan sesi keenam) ke halaman manapun yang di-mount setelahnya, termasuk `#home`. Fix: `pnm-universal.css` sekarang di-treat sama seperti stylesheet page-scoped lain — tetap di-load di `mount()` (lewat `ensureStyle()`, gak berubah), tapi sekarang di-`remove()` juga di `unmount()`. Browser HTTP cache bikin re-load berikutnya (kalau balik lagi ke `#konversian`) nyaris instant karena URL-nya persis sama (`?v=20260813b`), jadi gak ada biaya nyata. Font/icon links di `SHARED_LINKS` yang sama (Google Fonts, Phosphor, Tabler) SENGAJA TETAP nempel (gak dilepas) — cuma `pnm-universal.css` yang punya selector bare yang kebukti bocor.
   - **Belum diverifikasi pakai browser asli** (sandbox investigasi ini tetap gak punya akses ke domain CDN/font). Yang sudah dicek: `node --check` (syntax valid) di `konversian/index.js` (9021 baris). Sesi berikutnya/kamu sendiri: ulang skenario yang dilaporkan (buka `#konversian` → pindah ke `#home` atau `/stok.html` lalu balik) dan pastikan gak ada lagi tema kacau/blink.
   - **Catatan buat modul SPA baru ke depannya** (`crud-produk`/`dashboard`/`stok` kalau nanti dimigrasi dan juga butuh `pnm-universal.css`): ikuti pola yang sama — load di `mount()`, `remove()` di `unmount()` — jangan ulang pola "shared selamanya" yang baru saja dibongkar di sini.
2. **`kompres-pdf.html` dan `export-gambar-gabungan.html` (file legacy standalone) DIHAPUS** dari root, atas permintaan user setelah dikonfirmasi aman: di-grep menyeluruh dulu ke seluruh kodebase (`.js`/`.html`/`.md`), gak ada satupun referensi aktif ke dua file itu di luar komentar/dokumentasi (`app/pages/home/markup.js` udah pakai `#kompres-pdf`/`#export-gambar` hash-nav sejak sesi kedelapan, bukan lagi path `.html`-nya). `konversian.html`/`konversian.js` sendiri sudah lebih dulu dihapus user di sesi keempat (lihat catatan lama di `visi-pionir-workspaces.md` bagian 7). Tabel bagian 1 di atas diupdate reflect ini. Modul SPA-nya (`app/pages/kompres-pdf/`, `app/pages/export-gambar/`) TIDAK berubah sama sekali — cuma file legacy standalone-nya yang hilang.
   - Sisa file legacy standalone yang MASIH ada (sengaja, belum gilirannya): `stok.html`, `crud-produk.html`, `dashboard.html` — map.md bagian 4 urutan #4/#5 belum dikerjakan buat ketiganya.

**Update 23 Agustus 2026 (sesi kesepuluh):** Fix bug FOUC/flash — user kirim screen recording nunjukin `#home` sempet ke-render mentah (font default serif browser, icon SVG panah raksasa gak kebatasin ukurannya) sebelum CSS-nya kelar. Root cause: di SEMUA 4 halaman migrated (`home`, `kompres-pdf`, `export-gambar`, `konversian`), `ensureStyle()` nge-append `<link rel="stylesheet">` terus LANGSUNG lanjut set `container.innerHTML` tanpa nunggu link itu kelar di-download — jadi ada celah waktu (kebukti kerasa terutama di Vercel/koneksi lebih lambat dari localhost) di mana browser sempet paint markup mentah pakai default browser style, sebelum CSS beneran aktif.

Fix diterapkan konsisten ke keempat halaman: `ensureStyle()` di tiap halaman sekarang balikin `Promise` (resolve pas event `load` stylesheet-nya, atau langsung resolve kalau linknya udah ada duluan, atau tetap resolve — bukan reject — pas `error` biar gak nyangkut selamanya kalau CDN font/CSS gagal), dan `mount()` di tiap halaman sekarang `await` itu SEBELUM nge-set `innerHTML` (paralel bareng `ensureVendorScripts()` di 3 halaman yang punya vendor scripts, pakai `Promise.all`, jadi gak nambah waktu tunggu dibanding sebelumnya).

Diverifikasi: `node --check` di keempat file (`home/index.js`, `kompres-pdf/index.js`, `export-gambar/index.js`, `konversian/index.js` — 9037 baris), plus `python -m http.server` + `curl` pastiin semua masih ke-serve normal. **Belum diverifikasi hilang beneran di browser asli/Vercel** (sandbox ini gak punya akses CDN font buat ngetes render sungguhan) — sesi berikutnya/kamu sendiri: coba reload/navigasi ke tiap 4 halaman beberapa kali (terutama cold cache/hard refresh) dan pastikan gak ada lagi flash unstyled sebelum konten muncul.
**Update 23 Agustus 2026 (sesi kesebelas):** `crud-produk.html` (2928 baris `.js` + 752 baris `.html`) SELESAI dimigrasi ke `/app/pages/crud-produk/` — auth swap + SPA wrap dikerjakan sekaligus dalam satu sesi (bukan 2 sesi terpisah kayak yang disaranin bagian 4, ternyata gak perlu dipisah karena auth swap-nya kecil/targeted).

- **Pendekatan sama kayak `konversian`**, bukan kayak `kompres-pdf`/`export-gambar`: `document.getElementById(...)` di ratusan titik (380 `getElementById` + 17 `querySelectorAll` + 7 `querySelector`) TIDAK di-rescope ke `container.querySelector(...)` — dibiarkan apa adanya, aman karena cuma satu halaman yang termount di satu waktu (invariant yang dijamin `router.js`). Body asli `crud-produk.js` dibungkus hampir utuh jadi isi `mount(container)`, bukan direkonstruksi ulang.
- **Auth swap** (yang beda dari `konversian` — `crud-produk` sebelumnya "setengah jalan" kayak `export-gambar` dulu): 4 titik `sb.auth.signInWithPassword/signOut/onAuthStateChange` diganti ke `PNMAuth.login/logout/onAuthStateChange`. `PNMAuth.login()` throw kalau gagal (beda dari `sb.auth`'s `{error}` return), jadi call-site login form dibungkus try/catch. `sb.auth.refreshSession()` di tombol Bulk INAPROC Tracker SENGAJA TETAP dipakai langsung (forced server-verify sebelum batch panjang, beda dari `getSession()`/`getAccessToken()` yang cuma baca cache — `PNMAuth` belum punya wrapper buat ini, sama alasan `export-gambar` mempertahankan `sb.from('allowed_users')` langsung).
- **Bug scoping ketemu & diperbaiki**: `window.openEdit = async function(...)` (monkey-patch nambahin logic sembunyi-tab-kalau-tipe-SET) ngandelin classic-script sloppy-mode aliasing antara `window.foo` dan binding lokal `foo` — alias itu HILANG begitu kode pindah ke dalam function scope (`mount()`). Fix: `openEdit = ...` tanpa `window.` (reassign binding lokal langsung), biar semua pemanggil `openEdit(...)` lain di file yang sama (function scope yang sama sekarang) ikut kepakein versi yang di-wrap.
- **Theme-init `<script>` inline** (yang sebelumnya hidup di atas `<body>` di HTML asli, jalan sebelum body ke-parse) dipindah jadi kode JS beneran di awal `mount()` — `<script>` di dalam markup yang di-assign lewat `innerHTML` gak otomatis jalan (DOM quirk), sama pola persis kayak "inline script #1" di `konversian/index.js`.
- **4 listener document/window-level** (filter-popover outside-click, window resize, Ctrl+K keydown, `beforeunload`) di-nama-in (bukan anonymous lagi) dan ditangkep ke var module-scope biar `unmount()` bisa `removeEventListener` — beda dari listener yang nempel ke elemen DALAM container (otomatis ke-GC pas `container.innerHTML` dikosongin router.js). `beforeunload` ini yang paling penting dilepas — kalau kebiarin bakal ngecek elemen modal yang udah detached dari DOM begitu user pindah ke halaman SPA lain.
- **CSS**: sejak awal langsung dibikin FOUC-safe (`ensureStyle()` di-`await`, gak nunggu bugfix terpisah kayak 4 halaman lain) dan `pnm-universal.css` di-load-pas-mount/lepas-pas-unmount (pola yang sama kayak fix `konversian` sesi kesembilan/kesepuluh), bukan "shared selamanya". `tabler-icons` (v3.31.0) id+href DISAMAIN persis sama punya `konversian` biar beneran share satu `<link>`. Font JetBrains Mono id SENGAJA DIBEDAIN (`shared-jetbrains-mono-crud`) karena subset weight beda dari yang dipakai `konversian` (400;500 vs 400 doang) — kalau id disamain tapi href beda, salah satu halaman bakal dapet weight yang salah tergantung siapa yang mount duluan.
- **Navigasi lintas-halaman diperbarui** (kedua arah, karena `crud-produk` sekarang juga SPA):
  - `konversian/index.js`'s `navigateToEditProduk()`: target berubah dari `/crud-produk.html` (file standalone) ke `/` + `#crud-produk`. Query string (`?edit=...&return_to=konversian&return_sesi=...`) TETAP di luar hash (`crud-produk`'s `handleNavParamsIfAny()` baca `window.location.search`, bukan query di dalam hash) — genuine full-page navigation (`window.location.href`), bukan hashchange di dalam SPA yang sama, biar `window.location.search` valid dari fresh page load.
  - `crud-produk/index.js`'s tombol "Kembali ke Konversi": target `/app/shell.html` dibersihin jadi `/` (gak ada alasan lagi pakai `/app/shell.html` eksplisit sejak root jadi shell di sesi ketujuh — `/app/shell.html` masih hidup sebagai alias tapi gak dipakai lagi di titik ini).
- `router.js`: route `'crud-produk'` ditambahin ke `ROUTES`. `home/markup.js`: entri `crud-produk` di `MODULES` diubah `migrated: false` → `true`, `url` dari `/crud-produk.html` → `#crud-produk`.
- File baru: `app/pages/crud-produk/index.js` (~3130 baris), `markup.js` (body HTML asli, verbatim, gak ada backtick/`${}` yang bentrok sama template string), `style.css` (copy `crud-produk.css` apa adanya, 23 baris — hampir semua styling emang udah di `pnm-universal.css`).
- Diverifikasi: `node --check` di semua file yang disentuh (`router.js`, `home/markup.js`, `konversian/index.js`, `crud-produk/index.js`, `crud-produk/markup.js`), plus `python -m http.server` + `curl` — semua path baru 200, `crud-produk.html` (legacy standalone) juga dikonfirmasi MASIH ada (sengaja belum dihapus sesi ini, beda dari `kompres-pdf.html`/`export-gambar-gabungan.html` yang udah dihapus sesi kesembilan — alasan beda: dua itu sempet dites dulu beberapa sesi sebelum dihapus, `crud-produk` baru migrasi sesi ini juga, belum ada kesempatan dites user).
- **Belum diverifikasi browser asli/Vercel** — migrasi terbesar dan paling banyak titik sentuh (2928+752 baris, 8 patch bedah + auth swap) sejauh ini, jadi risiko regresi tersembunyi lebih tinggi dari 3 migrasi sebelumnya walau sudah dicek `node --check` dan path-level di semua file. **Sesi berikutnya/kamu sendiri WAJIB test end-to-end di browser sebelum hapus `crud-produk.html` legacy**: login, lihat daftar Produk, buka/edit 1 produk (termasuk tipe SET — buat mastiin fix `openEdit` scoping beneran jalan), Bulk Edit (INAPROC/Harga/Link V6 — mastiin `refreshSession()` yang sengaja gak diubah masih jalan), Ctrl+K search, navigasi PP → dan balik dari `#konversian`, tutup tab pas ada perubahan belum disimpan (mastiin `beforeunload` masih muncul saat lagi di halaman ini DAN gak muncul salah nyasar pas udah pindah halaman).
- Sisa legacy standalone yang MASIH ada: `stok.html`, `dashboard.html` — bagian 4 urutan #5 belum dikerjakan buat keduanya.
**Update 24 Agustus 2026 (sesi kedua belas):** Bugfix atas laporan user langsung setelah deploy sesi kesebelas — `Cannot read properties of undefined (reading 'from')` di berbagai fungsi (`loadProduk`, `checkWhitelistAndShowApp`, dll), semuanya nunjuk ke `sb` yang `undefined`.

Root cause: `const sb = window.pnmSupabase;` ke-taro di MODULE SCOPE (di luar `mount()`), padahal module ini di-`import()` `router.js` SEBELUM `ensureVendorScripts()` sempat jalan — beda dari classic-script asli, di mana urutan `<script>` tag di `<head>` udah menjamin `shared/supabase-client.js` (yang nyetel `window.pnmSupabase`) kelar duluan sebelum `crud-produk.js` sendiri dievaluasi. Konsekuensinya `window.pnmSupabase` masih `undefined` pas baris itu dieksekusi, jadi `sb` ke-assign `undefined` selamanya (module cuma dievaluasi sekali, gak ke-re-run tiap `mount()` dipanggil).

Fix: `const sb = window.pnmSupabase;` dipindah ke DALAM `mount()`, tepat setelah `await Promise.all([ensureStyle(), ensureVendorScripts()])` — di titik itu `window.pnmSupabase` dijamin udah ada. Ini murni pemindahan 1 baris (plus komentar penjelasan) — gak ada logic lain yang berubah.

Diverifikasi: `node --check` (syntax valid), `python -m http.server` + `curl` (file masih ke-serve). **Belum diverifikasi browser asli** — sesi berikutnya/kamu sendiri: ulang alur yang tadi eror (login → lihat daftar Produk) dan konfirmasi `sb.from()` sekarang kebaca bener.

**Catatan penting buat sesi migrasi SPA berikutnya (`stok.html`/`dashboard.html`)**: cek dulu apakah file itu juga punya pola `const sb = window.pnmSupabase` (atau serupa) di scope PALING LUAR file — kalau ada, WAJIB dipindah ke dalam `mount()` setelah `ensureVendorScripts()`, jangan dibiarkan di module scope. Ini bug kelas baru yang belum kejadian di `konversian`/`kompres-pdf`/`export-gambar` (mereka gak punya pola assignment module-scope kayak gini), jadi gampang kelewat kalau cuma nyalin pola lama.
**Update 24 Agustus 2026 (sesi keempat belas):** User upload `design.md` (spec desain flat/neutral — token warna, spacing, komponen shared nav, dibuat biar semua halaman konsisten visual, saat ini masing-masing halaman punya gaya sendiri-sendiri). File disimpan di root repo.

**Keputusan urutan kerja** (dibahas eksplisit sama user): BUKAN "migrasi stok+dashboard dulu pakai gaya lama, baru reskin semua belakangan" — itu bikin stok/dashboard dikerjain 2x (checklist migrasi `design.md` sendiri naruh langkah desain SEBELUM wire-up logic/router, urutan #7 baru "wire up actual route/JS logic"). Sebagai gantinya:
1. Bangun shared nav + token set dulu (foundational, gak nyentuh 5 halaman yang udah jalan) — SESI INI.
2. Migrasi `stok`/`dashboard` LANGSUNG pakai shared nav + token baru (bukan gaya lama dulu terus direskin).
3. Retrofit 5 halaman yang udah jalan (`home`/`konversian`/`crud-produk`/`kompres-pdf`/`export-gambar`) ke desain baru — sesi terpisah, belakangan, setelah desain sistemnya kebukti jalan di 2 halaman baru dulu.

**Dibangun sesi ini**: `app/pages/_nav/` (folder baru, 3 file) — `nav.css`, `markup.js`, `nav.js`.

- **Token warna**: persis nilai dari `design.md`, TAPI di-scope ke class `.pw-shell` (bukan `:root` global) — alasannya dijelasin di komentar header `nav.css`: `pnm-universal.css` udah punya `--bg`/`--accent` sendiri di `:root` dengan NILAI BEDA (Apple-blue `#007AFF` vs Vercel-blue `#0070F3` punya `design.md`). Nama variabel SENGAJA TETAP nama asli `design.md` (`--bg`, `--accent`, dst — bukan diprefix `--pw-bg` dkk) karena nama-nama itu emang dimaksud jadi token GLOBAL kanonik nantinya setelah merge — scoping lewat SELECTOR (`.pw-shell`), bukan lewat prefix nama variabel, biar gak nambah token set ketiga yang malah nambah masalah yang lagi coba diberesin `design.md`.
- **Ikon**: pakai `@tabler/icons-webfont@3.31.0` — versi PERSIS SAMA yang udah kepakai di `konversian`/`crud-produk` (`shared-tabler-icons`), bukan versi/id baru, biar beneran share satu `<link>` kalau ketiga-tiganya kebetulan aktif bareng. Mapping icon per modul (`NAV_ICONS` di `markup.js`) dibikin baru khusus nav ini — bukan pakai `MODULES[].icon` (SVG gambar-tangan per-modul) atau `MODULES[].color` dari `home/markup.js`, karena `design.md` eksplisit nyebut itu ANTI-PATTERN UTAMA yang mau dihapus.
- **`MODULES` gak di-duplikat** — `markup.js` di sini `import { MODULES } from '../home/markup.js'`, biar daftar modul (id/title/url/migrated) tetap SATU sumber kebenaran, bukan taro 2 tempat yang bisa kesasar beda.
- **Lifecycle beda dari halaman biasa**: nav ini punya `navMount()`/`navSetActive()`, BUKAN `mount()`/`unmount()` kayak modul `app/pages/<nama>/` lain — karena nav dimaksud HIDUP TERUS lintas route change (`design.md`: "Lives in app/shell.html... not per-page"), bukan di-remount tiap hashchange kayak halaman biasa.
- **SENGAJA BELUM di-wire ke `app/router.js`/`app/shell.html`** — dicek `md5sum` kedua file itu SAMA PERSIS kayak sebelum sesi ini, gak disentuh sama sekali. Alasan: nge-aktifin nav sekarang bakal langsung geser/pengaruhin LAYOUT ke-5 halaman yang udah jalan (mereka semua asumsi full-width, belum ada margin buat sidebar 200px) — itu justru ngelanggar urutan kerja yang baru disepakatin di atas (retrofit 5 halaman itu belakangan, sesi terpisah). Instruksi lengkap cara wire-in (3 langkah, buat sesi migrasi stok/dashboard nanti) ada di komentar header `nav.js`.
- Diverifikasi: `node --check` di `markup.js`/`nav.js`, brace-balance check di `nav.css`, `python -m http.server` + `curl` (semua 3 file 200). **Belum diverifikasi browser asli** (wajar — belum di-wire ke halaman manapun juga, gak ada yang bisa dirender sampai langkah wiring di atas dikerjain).
**Update 24 Agustus 2026 (sesi kelima belas):** `dashboard.html` SELESAI dimigrasi ke `/app/pages/dashboard/` — dan shared nav (`app/pages/nav/`, dibangun sesi keempat belas tapi belum di-wire) SEKARANG BENERAN AKTIF buat pertama kalinya. Sesi besar, banyak bagian:

**Keputusan user eksplisit sebelum sesi ini**: konfirmasi "stick to design.md principle" berarti nerima konsekuensi 5 halaman yang udah jalan (`home`/`konversian`/`crud-produk`/`kompres-pdf`/`export-gambar`) BAKAL butuh retrofit lengkap ke design system nanti (terpisah, belakangan) — bukan cuma migrasi stok/dashboard doang. Atas dasar itu, nav diaktifin SEKARANG (bukan ditunda lagi).

**Wiring nav (infra, bukan halaman spesifik)**:
- `app/shell.html`/`index.html`: `<div id="app">` dibungkus jadi `<div class="pw-shell"><nav id="app-nav"></nav><div id="app" class="pw-content"></div></div>` — kedua file dijaga identik persis (established convention sejak sesi ketujuh).
- `app/router.js`: `import { navMount, navSetActive } from './pages/nav/nav.js'`, dipanggil di awal `render()` (nav mount SEKALI, `navSetActive()` tiap route berubah) SEBELUM halaman lama di-unmount/halaman baru di-mount — urutan ini penting biar nav gak nunggu halaman kelar dulu.

**BUG SERIUS ketemu & difix SEBELUM sempet di-push** (bukan ketauan dari laporan user, ketauan sendiri pas ngerjain dashboard dan mikirin ulang implikasi wiring-nya): token warna `design.md` awalnya di-scope ke `.pw-shell` (dibangun sesi keempat belas). Karena `.pw-shell` sekarang jadi ANCESTOR dari `#app` buat SEMUA halaman (bukan cuma dashboard), dan `pnm-universal.css`'s `:root` pakai NAMA VARIABEL YANG SAMA PERSIS (`--bg`, `--accent`, `--border`, `--text`, dst) dengan NILAI BEDA — CSS custom property cascade turun ke semua descendant, jadi kalau dibiarin, `konversian`/`crud-produk`/`export-gambar` (yang semua pakai `var(--accent)` dkk dari `pnm-universal.css` secara ekstensif) bakal DIAM-DIAM KEWARISAN warna baru `design.md`, TANPA file mereka sendiri disentuh sama sekali. Ini jauh lebih parah dari yang udah di-disclose ke user sebelumnya ("cuma bakal kesempitan sidebar 200px") — nyaris ke-ship sebagai regresi visual diam-diam ke 3 halaman yang udah jalan.
- **Fix**: token dipindah dari `.pw-shell` ke `.pw-nav`+`.pw-topbar` SAJA (dua-duanya SIBLING dari konten halaman, bukan ancestor-nya — lihat struktur `<div id="app-root"><div class="pw-topbar">...</div><main>...</main></div>`, `.pw-topbar` gak jadi ancestor `<main>`). `.pw-shell`/`.pw-content` sekarang PURE LAYOUT (flex/sizing doang), nol asumsi warna.
- Komentar di `nav.css`/`nav.js` diupdate reflect status WIRED IN + histori bug ini, biar sesi berikutnya yang mau nambah selector baru ke file itu ngerti kenapa token di-scope ke situ, bukan ke `.pw-shell`.

**Migrasi `dashboard.html` sendiri** (925... eh 336 baris HTML + 1295 baris JS):
- **Auth**: BEDA SENDIRI dari 3 halaman sebelumnya — file asli SAMA SEKALI GAK PAKAI SDK Supabase, murni `fetch()` manual ke `/auth/v1/token`, token disimpan manual di localStorage, didekode manual dari JWT. Seluruh layer ini diganti ke `PNMAuth.*` (bukan sekadar swap panggilan kayak `crud-produk`). Gak ada whitelist check (`allowed_users`) di file asli — DIPERTAHANKAN apa adanya (bukan keputusan sesi ini buat nambah), dicatat sebagai observasi buat didiskusiin nanti kalau mau disamain sama `crud-produk`.
- **rpc()** helper (satu-satunya titik semua data fetching): token dari variable lokal jadi `await PNMAuth.getAccessToken()` fresh tiap call — fix persis sama kelasnya kayak `getFreshToken()` di `konversian`.
- **2 bug nyata ketemu pas baca kode buat porting** (bukan disengaja dibikin):
  1. Theme toggle baca/tulis `document.body.getAttribute('data-theme')`, tapi `pnm-universal.css` cuma react ke `<html data-theme>` — tombol keliatan "jalan" (icon ganti) tapi WARNA HALAMAN GAK PERNAH BENERAN BERUBAH. Ternyata ADA JUGA toggle kedua yang dobel (inline `<script>` standalone di `dashboard.html`, komentarnya sendiri bilang "works even if dashboard.js omits it") — dua-duanya kepasang ke tombol yang sama, race/dobel-toggle. **Fix: dihapus TOTAL** (bukan diperbaiki) — nav yang sekarang WIRED IN udah punya toggle-nya sendiri yang bener, dashboard gak perlu punya lagi.
  2. Wiring detail-modal (`detailOverlay`, tombol close, dll) dibungkus `document.addEventListener('DOMContentLoaded', ...)` — event itu cuma nembak SEKALI per real page load. Di SPA ini, `mount()` bisa kepanggil berkali-kali (tiap user hash-navigate balik ke `#dashboard`) — listener itu gak akan PERNAH jalan lagi di kunjungan ke-2+, `detailOverlay` tetap `undefined` selamanya. **Fix: dijalanin langsung** (bukan nunggu event), sama pola kayak fix serupa di halaman lain.
  - Ketemu juga: patch pertama buat fix #2 SEMPAT ninggalin bug baru (assignment ke `detailOverlay` tanpa `let`/`const` — bakal `ReferenceError` di strict mode/ES module) — ketauan & difix sebelum lanjut, bukan kelolos.
- **24 fungsi** (bukan 23 seperti sempet salah ketulis di draft komentar awal, sudah dikoreksi) di-expose ke `window.*` di akhir `mount()`, di-`delete` di `unmount()` — markup lama penuh inline `onclick="..."`/`onchange="..."` yang nunjuk fungsi-fungsi ini, sama kelas fix kayak `window.openEdit` di `crud-produk` tapi jauh lebih banyak titiknya di sini (list lengkap ada di komentar `index.js`).
- **`document.getElementById` TIDAK di-rescope** ke `container.querySelector` — sama kebijakan kayak `konversian`/`crud-produk`, aman selama satu halaman per waktu.
- **design.md structural step 1 doang** yang dikerjain buat konten dashboard sendiri: `<header>` lama (logo/brand + link "Beranda" + theme toggle) diganti `.pw-topbar` (breadcrumb + tombol "Muat ulang" yang genuinely page-specific dipindah ke situ). **Steps 2-6 (token warna/spacing/icon per-komponen) SENGAJA BELUM dikerjain** — `.stat-card.blue` dkk (salah satu anti-pattern yang disebut `design.md` sendiri) MASIH dipakai apa adanya buat konten internal (stat cards, tabel, chart). Scope sesi ini udah besar (auth rewrite + 24 fungsi + wiring nav pertama kali) — desain ulang tiap komponen internal itu kerjaan terpisah, lebih baik gak ditumpuk.
- **Gak ada `style.css` sendiri** — beda dari 4 halaman migrated sebelumnya, `dashboard.html` asli emang gak punya stylesheet sendiri (semua styling-nya udah di `pnm-universal.css`), jadi `ensureStyle()` di `index.js` gak coba nge-load file yang gak ada.
- `shared-google-fonts`/`shared-phosphor-icons`: id+href dicek dulu SAMA PERSIS kayak yang dipakai `konversian`, dikonfirmasi identik sebelum nulis, jadi beneran share satu `<link>`.
- `home/markup.js`: entri `dashboard` di `MODULES` diubah `migrated: false` → `true`, url `/dashboard.html` → `#dashboard`. `router.js`: route `'dashboard'` ditambahin ke `ROUTES`.

Diverifikasi: `node --check` di semua file JS yang disentuh/dibuat, brace-balance check `nav.css`, `python -m http.server` + `curl` (semua path 200). **Belum diverifikasi browser asli** — sesi ini paling banyak titik sentuh sejauh ini (wiring infra + migrasi halaman + 2 bug fix + 1 bug ketemu-sendiri-sebelum-ship), jadi risiko regresi tersembunyi lebih tinggi dari migrasi-migrasi sebelumnya. **WAJIB test end-to-end di browser sebelum hapus `dashboard.html` legacy**: login, cek SEMUA 24 fungsi onclick (filter, search, preset, export CSV/Excel, pagination, detail modal buka/tutup, wordtree, forecast period), DAN — penting — buka `#konversian`/`#crud-produk`/`#home` di tab yang sama abis ini dan pastiin WARNANYA GAK BERUBAH (verifikasi manual buat bug token-leak yang tadi difix, sandbox ini cuma bisa cek lewat baca CSS, gak bisa render beneran).

Sisa legacy standalone: `stok.html` doang.

**Update 26 Agustus 2026 (sesi keenam belas):** User test manual (sandbox lokal, `python -m http.server`) atas hasil sesi kelima belas, 3 screenshot dikirim (`CRUD Produk`, `Conversion Workspace`, `Home`). Feedback: "overall its still sloppy", dan secara eksplisit minta home dipensiunkan karena sidebar persisten bikin daftar modul di halaman home jadi redundant ("if we have persistent nav on the side its like poinless to show again the list of the tools").

**3 perbaikan dikerjain, dipilih berdasarkan risk/effort — bukan tuntas semuanya sekaligus:**

1. **Route `home` DIPENSIUNKAN.** Dihapus dari `ROUTES` di `router.js`, `DEFAULT_ROUTE` diganti `'home'` → `'dashboard'` (lebih informatif buat landing — data asli, bukan angka hardcoded kayak stats home yang lama). `app/pages/home/` TIDAK dihapus dari disk — sengaja dibiarin ada, cuma jadi "yatim"/unreachable (sama kehati-hatian kayak file legacy `.html` yang gak langsung dihapus sampai kebukti aman). `nav-markup.js` MASIH `import { MODULES } from '../home/markup.js'` — itu tetap jalan normal, karena yang diambil cuma data array `MODULES`-nya, bukan halaman/route home-nya sendiri, jadi retire route ini gak mempengaruhi sidebar sama sekali. Bookmark/link lama ke `#home` gak jadi dead-link — ditambahin `RETIRED_REDIRECTS` di `router.js` yang otomatis lempar ke `#dashboard`.

2. **`konversian`'s top bar sendiri (Beranda/Stok/theme-toggle/Keluar) DISEMBUNYIKAN lewat CSS**, bukan dihapus dari markup/`index.js` — keempatnya sekarang 100% duplikat sama shared nav. Pendekatan CSS-only (`display:none` di `style.css`, `#app-root .header-right ...`) dipilih SENGAJA karena `konversian/index.js` (9000+ baris) itu file paling gede & paling delicate di app ini dan belum full browser-tested sejak beberapa ronde perubahan terakhir — bongkar markup+listener JS buat menangnya cosmetic doang itu risiko yang gak sepadan. Elemen/id/listener klik di `index.js` semuanya MASIH UTUH, cuma gak dirender. Kalau nav bersama ini suatu saat dilepas lagi, cukup hapus blok CSS itu buat balikin header lama persis kayak semula.

3. **Logout ditambahin ke shared nav** (`pw-nav-user` row, tombol baru `pwNavLogoutBtn`, ikon `ti-logout`) — prasyarat buat langkah #2 di atas (kalau `Keluar` di `konversian` disembunyiin tanpa ganti, user kehilangan satu-satunya jalan logout). Manggil `window.PNMAuth.logout()`, dicek FRESH pas diklik (bukan pas mount) karena `window.PNMAuth` mungkin belum ke-load pas nav pertama kali mount (nav mount duluan sebelum halaman manapun, termasuk vendor script-nya, selesai) — kalau belum ada, tombol diam aja (no-op), gak nge-throw.

**SENGAJA BELUM disentuh** (di luar scope sesi ini, terlalu berisiko buat dikerjain blind tanpa browser): `crud-produk`'s sidebar internal ("Product Manager", PNM/SMY/METO, Dashboard/Produk/Set Management/AKD/KFA Management/Bulk Edit) MASIH dobel-tampil bareng shared nav — beda dari kasus `konversian`, ini BUKAN 1:1 duplikat konten (isinya beda: sub-navigasi DI DALAM modul crud-produk, bukan navigasi ANTAR modul), jadi solusinya bukan "sembunyiin doang" tapi butuh keputusan desain (collapse default? gabung ke topbar jadi tabs? dst) — ditunda ke sesi terpisah. Catatan buat sesi itu: sidebar `crud-produk` UDAH PUNYA tombol collapse sendiri (`#sidebarCollapseBtn`) — user bisa pakai itu manual buat ngurangin clutter sementara, sambil nunggu keputusan desain yang lebih permanen.

Diverifikasi: `node --check` di `router.js`/`nav.js`/`nav-markup.js`, brace-balance check `konversian/style.css`, `python -m http.server` + `curl` (semua path 200). **Belum diverifikasi browser asli** — terutama fix `display:none` di `konversian` (gak bisa dipastikan visual beneran rapi tanpa render sungguhan) dan urutan redirect `#home` → `#dashboard` (logic-nya kelihatan benar dibaca, tapi belum dicoba klik beneran).
**Update 26 Agustus 2026 (sesi ketujuh belas):** `stok.html` SELESAI dimigrasi ke `/app/pages/stok/` -- halaman TERAKHIR dari 6, migrasi SPA nya sekarang selesai secara struktural (browser end-to-end testing masih nunggak buat 3 halaman, lihat catatan di bawah).

**Beda dari 5 halaman sebelumnya**: `stok.html` masih inline `<script>` di dalam file HTML-nya sendiri (bukan file `.js` terpisah kayak `dashboard.js`/`crud-produk.js`) -- jadi sesi ini juga ngerjain satu langkah restrukturisasi tambahan yang disebut di `map.md` (pisahin markup dari script) sebelum migrasi SPA yang biasa (auth swap, `mount()`/`unmount()`, dst).

**Auth**: sama persis kelasnya kayak `dashboard.html` asli -- SAMA SEKALI GAK PAKAI SDK Supabase, murni `fetch()` manual ke `/auth/v1/token`, token+refresh_token disimpan manual di localStorage (key `pnm_auth_session` -- kebetulan SAMA PERSIS sama key yang dipakai `shared/supabase-client.js` sekarang, jadi user yang lagi login di `stok.html` versi lama otomatis kebaca sesinya begitu buka versi SPA, gak perlu re-login), email di-decode manual dari JWT. Seluruh layer ini diganti ke `PNMAuth.*`. `stokCurrentEmail` (dipakai buat catat "siapa yang upload" di riwayat) sekarang diambil dari `session.user.email` di `onAuthStateChange` callback, bukan didekode manual dari JWT lagi -- pola yang sama kayak `crud-produk`'s `checkWhitelistAndShowApp(session)`.

**4 titik `fetch()` manual** (`fetchSharedHistory`, `fetchSkippedCodesForRow`, `insertSharedHistory`, `fetchValidKodeSet`) plus 1 titik upload final (RPC `replace_stok_produk`) semuanya diganti dari token variable lokal (`stokAccessToken`, gampang basi kalau tab dibackground) ke `await PNMAuth.getAccessToken()` dipanggil FRESH tiap request -- fix persis sama kelasnya kayak `getFreshToken()` di `konversian` dan `rpc()` di `dashboard`.

**Duplikasi `const SUPABASE_URL`/`ANON_KEY` ditutup beneran** -- ini item yang eksplisit disebut `map.md` (bagian 1, sebelum sesi ini) sebagai duplikasi yang masih tersisa khusus di `stok.html`/`dashboard.html`. Beda dari `dashboard`/`crud-produk` (yang migrasinya tetap nge-declare ulang `const SUPABASE_URL` lokal, gak sempet dibereskan sesi masing-masing), sesi ini pakai `window.PNM_SUPABASE_URL`/`window.PNM_SUPABASE_ANON_KEY` dari `shared/supabase-client.js` langsung -- satu-satunya halaman lain yang udah ngelakuin ini adalah `export-gambar`. Diisi di dalam `mount()` setelah `ensureVendorScripts()` resolve, bukan di module scope (sama alasan kenapa `const sb = window.pnmSupabase` juga gak boleh di module scope -- item checklist `map.md` yang sama).

**Theme**: `stok.html` asli punya 2 tombol theme-toggle sendiri (`#theme-toggle-gate` di auth-gate, `#theme-toggle-app` di dalam app), pakai localStorage key `pnm_theme` (BEDA dari key `theme` yang dipakai shared nav & `dashboard`). Dibangun SESUDAH nav udah wired in (beda dari `crud-produk`/`konversian` yang lebih tua), jadi ngikutin pola `dashboard`: KEDUA tombol itu DIHAPUS TOTAL dari markup, bukan diperbaiki/disamain key-nya -- shared nav sekarang satu-satunya pemilik theming, key `theme` yang dipakai. Sisa satu baris theme-init IIFE di `mount()` (baca `localStorage.getItem('theme')`, set `<html data-theme>`) buat jaga-jaga cegah flash tema salah kalau `#stok` kebetulan halaman pertama yang ke-mount di satu page load -- pola sama persis kayak `dashboard/index.js`.

**Gak ada `style.css` sendiri** -- dicek DULU class-per-class (`.auth-gate`, `.dropzone`, `.file-row`, `.checking-row`, `.stat-pill`, `.skip-panel`, `.skip-table`, `.actions-row`, `.btn-primary-lg`, `.btn-secondary`, `.result-banner`, `.activity-table`, `.act-when`, `.act-user`, `.act-dl`, `.local-note`, `.page-head`, `.card`, `.card-head`, `.icon-box`, `.summary-row`, `.summary-main`, `.metric-group`, `.metric-icon`, dst) sebelum nulis kode, SEMUANYA udah ada di `pnm-universal.css` (komentar di kepala file itu sendiri bilang "Digabung dari: ... stok.html (inline)" -- CSS-nya udah pernah di-merge duluan, cuma script-nya yang belum). Jadi TIDAK disalin `:root` token block punya `stok.html` yang lama (nilai hex beda dari token `pnm-universal.css`) ke mana pun -- itu bakal jadi `:root` override global yang bocor ke SEMUA halaman begitu link-nya pernah dimuat (gak kayak `pnm-universal.css` sendiri yang dilepas lagi pas `unmount()`), persis kelas bug "token-leak" yang komentar `nav.css` bilang udah ketemu & difix pas nav dibangun (sesi kelima belas) -- dihindari dari awal di sini, bukan ketemu lagi terus difix.

**Icon**: `stok.html` asli load DUA stylesheet phosphor (`regular` dan `fill`), tapi dicek lagi -- markup-nya cuma pakai class `ph-*` (regular), gak ada satupun `ph-fill`. Set `fill` yang mubazir itu gak diikutkan di `SHARED_LINKS`. `shared-google-fonts`/`shared-phosphor-icons` pakai id+href SAMA PERSIS kayak punya `dashboard` (dicek dulu, bukan asumsi) -- beneran share `<link>` yang sama kalau dua-duanya kebetulan aktif bareng. Catatan kecil: `stok.html` asli sebenernya gak pernah load webfont Inter sama sekali (gak ada `<link>` Google Fonts di `<head>` aslinya, walau CSS-nya nyebut `font-family:'Inter'` -- diam-diam fallback ke `system-ui`) -- sesi ini nambahin link Inter yang sama kayak `dashboard` pakai, ngurangin gap kecil itu sebagai bonus konsistensi, bukan permintaan eksplisit.

**Gak ada inline `onclick`/`onchange`/`oninput` sama sekali** di markup asli `stok.html` (semua wiring `addEventListener` di script) -- dicek dulu sebelum nulis `markup.js`. Beda dari `dashboard` (24 fungsi ke-expose ke `window.*`) dan `crud-produk`, `index.js` halaman ini TIDAK PERLU expose fungsi apapun ke `window.*`, dan `unmount()` gak perlu `delete window.foo` sama sekali.

**Link internal diupdate**: `konversian/markup.js`'s top bar (`href="/stok.html"` -> `href="#stok"`) dan `home/markup.js`'s `MODULES` entry `stok` (`url: '/stok.html'` -> `'#stok'`, `migrated: false` -> `true`) -- walau route `home` sendiri retired/orphaned dari `ROUTES` (sesi keenam belas), datanya (`MODULES` array) masih dipakai `nav-markup.js` buat sidebar, jadi tetap worth diupdate buat akurasi kalau suatu saat dibaca lagi.

**`document.getElementById` TIDAK di-rescope** ke `container.querySelector` -- kebijakan sama kayak semua halaman lain, aman selama satu halaman per waktu (dijamin `router.js`). State module-scope (`stokCurrentEmail`, `sharedHistoryAvailable`, `historyShowAll`, `currentFileName`, `stokParsedRows`, `stokMatchedRows`, `stokSkippedRows`) di-reset eksplisit di AWAL tiap `mount()` -- bukan cuma dideklarasikan sekali di module scope -- biar gak ada sisa state upload dari kunjungan sebelumnya nongol lagi kalau user hash-navigate keluar-masuk `#stok` berkali-kali dalam satu page load yang sama.

Diverifikasi: `node --check` di `stok/index.js`, `stok/markup.js`, `router.js`, `konversian/markup.js`, `home/markup.js` -- semua OK. Class CSS dicek satu-satu lewat `grep` ke `pnm-universal.css`, bukan asumsi. **Belum diverifikasi browser asli** -- sama seperti `crud-produk`/`dashboard`, sandbox ini gak bisa render beneran. **WAJIB test end-to-end di browser sebelum hapus `stok.html` legacy**: login, upload file Excel (kolom KODEASLI/QTY), cek precheck panel (matched/skipped), download CSV skipped, upload final ke `replace_stok_produk`, cek riwayat (shared table DAN fallback localStorage kalau tabel belum ada), refresh riwayat, "Lihat Semua"/"Ringkas" toggle, download log skipped per baris riwayat -- DAN, penting, buka `#dashboard`/`#konversian`/`#crud-produk` di tab yang sama abis ini, pastiin WARNANYA GAK BERUBAH (verifikasi manual token-leak, gak ada yang disalin dari `:root` lama tapi tetap worth dicek beneran di browser).

Dengan ini, ke-6 halaman legacy (`konversian`/`crud-produk`/`dashboard`/`stok`/`kompres-pdf`/`export-gambar`) semuanya punya versi SPA. Sisa kerja besar yang belum disentuh: browser end-to-end testing buat `crud-produk`/`dashboard`/`stok` (3 halaman, belum pernah sama sekali) sebelum hapus file legacy-nya; retrofit visual penuh ke `design.md` (steps 2-6 -- token warna/spacing/icon/status-dot -- belum dikerjain di halaman manapun, cuma step 1/structural yang udah di `dashboard`+`stok`); beresin dobel sidebar `crud-produk` (lihat entri sesi keenam belas di atas).
**Update 26 Agustus 2026 (sesi kedelapan belas):** Bug report user: CSS "nyangkut" pas pindah dari `#export-gambar` ke modul lain (warna/tema ikut kebawa). Diminta cek dulu apa ini varian bug sesi kesembilan (`pnm-universal.css` gak pernah `remove()` di `unmount()`) atau sesi kesepuluh (`ensureStyle()` gak di-`await` sebelum `innerHTML` di-set), bandingin sama `dashboard`/`crud-produk` yang aman.

**Root cause -- persis kelas bug sesi kesembilan, BUKAN sesi kesepuluh:** `export-gambar/index.js` sudah benar soal `await Promise.all([ensureStyle(), ensureVendorScripts()])` (fix sesi kesepuluh sudah ada dari awal, gak ada regresi di situ). Yang salah: halaman ini gak pakai `pnm-universal.css` sama sekali (beda dari 4 halaman lain), tapi PUNYA `style.css` sendiri (tema "dark glass" khusus halaman ini, di-link sebagai `page-export-gambar-style`) yang: (1) ada bare/unscoped selector asli -- `:root{...}` (token warna override), `html,body{...}`, `body::before`/`body::after` (background gradient + noise layer), `h1{...}`, `select{...}` -- dan (2) TIDAK PERNAH di-`remove()` di `unmount()`-nya. Dibandingkan `dashboard`/`crud-produk`/`stok`/`konversian`, yang KEEMPATNYA benar manggil `document.getElementById('shared-pnm-universal-css')?.remove()` di `unmount()` masing-masing (pola fix sesi kesembilan). `export-gambar` gak punya baris itu SAMA SEKALI buat stylesheet-nya sendiri -- begitu user pernah mampir ke `#export-gambar`, link `page-export-gambar-style` nempel selamanya di `<head>`, dan token/body-override/h1-nya bocor ke halaman apapun yang di-mount setelahnya.

**Fix**: tambahin `document.getElementById('page-export-gambar-style')?.remove();` di `unmount()` (satu baris, pola identik dengan 4 halaman lain).

**Temuan tambahan yang PENTING dilaporkan (BELUM difix sesi ini -- di luar scope yang diminta user, "cuma fix bug di atas"):** pas audit dibandingin ke semua halaman buat mastiin gak ada varian bug yang sama kelewat, ketemu 2 halaman lain yang PUNYA POLA IDENTIK (stylesheet page-specific gak pernah di-`remove()` di `unmount()`), beda tingkat keparahan:

1. **`kompres-pdf/index.js` -- SAMA PARAHNYA dengan bug `export-gambar` di atas.** `style.css`-nya juga punya bare selector asli: `*{box-sizing:border-box}`, `html,body{margin:0;padding:0}`, `body{...}` (background + font + padding override), `h1{...}`. `unmount()`-nya (`mountedContainer?.classList.remove('wrap'); mountedContainer = null;`) TIDAK PERNAH `remove()` link `page-kompres-pdf-style`-nya. Kalau ini gak dibetulin juga, kombinasi pindah `#kompres-pdf` -> modul lain bakal kena bug PERSIS SAMA yang baru aja dilaporin user buat `export-gambar`, cuma belum pernah kejadian/keluhan (mungkin karena kompres-pdf jarang dipakai, atau warnanya kebetulan gak terlalu beda kentara). **Rekomendasi: fix sekarang juga (satu baris, resiko sama rendahnya dengan fix export-gambar di atas), tapi SENGAJA belum dieksekusi karena scope yang diminta eksplisit cuma export-gambar -- tunggu konfirmasi.**
2. **`crud-produk/index.js` -- lebih ringan, tapi tetap leak nyata.** `style.css`-nya cuma punya 3 bare selector: `table{...}`, `th{...}`, `td{...}` (width/padding/border/warna). Gak nyentuh `:root`/`body`/`h1`, jadi dampaknya kebatas ke elemen `<table>` di halaman lain (misalnya kalau `dashboard` atau `konversian` punya tabel, border/padding-nya bisa ikut kebawa gaya `crud-produk` kalau user pernah mampir ke sana duluan). `unmount()`-nya juga gak pernah `remove()` `page-crud-produk-style`. Prioritas lebih rendah dari (1), tapi kelas bug yang sama.

Halaman yang KEBUKTI aman meninggalkan stylesheet page-specific-nya ke-load selamanya (gak perlu di-`remove()`): `konversian` (`style.css`-nya di-scope penuh di bawah `#app-root`, dicek nol bare selector) dan `dashboard`/`stok` (gak punya `style.css` sendiri sama sekali, semua dari `pnm-universal.css` yang sudah benar di-`remove()`).

**Testing**: sandbox investigasi sesi ini SAMA SEPERTI sesi kesembilan -- gak ada akses network ke CDN (`jsdelivr.net`, `unpkg.com`) maupun project Supabase, jadi gak ada cara jalanin browser/Playwright asli dari sini. Yang SUDAH dicek: `node --check` di `export-gambar/index.js` + `markup.js` (syntax valid), audit manual (bukan asumsi) ke SEMUA 6 `style.css` modul buat cari bare selector (`:root`, `html`, `body`, `h[1-6]`, `input[type=...]`, `select`, `table`/`th`/`td`, `*`) DAN cross-check tiap `unmount()` modul buat mastiin link CSS-nya kena `remove()` atau emang terbukti aman ditinggal (tabel di atas). **Kombinasi pindah-pindah antar 6 modul BELUM divalidasi di browser asli** -- terutama urutan yang diminta user (`export-gambar` -> `konversian` -> `stok` -> `dashboard`, dst) dan urutan yang sama sekali baru buat `kompres-pdf`/`crud-produk`. **WAJIB test manual di browser sebelum push ke main**, sama seperti disclaimer di sesi-sesi migrasi sebelumnya buat 3 halaman yang belum pernah dites (`crud-produk`/`dashboard`/`stok`) -- sekarang ditambah kombinasi CSS-leak spesifik ini buat SEMUA 6 halaman, bukan cuma `export-gambar`.

**Status push ke main**: bug yang diminta user (`export-gambar`) SUDAH difix. TAPI ada temuan `kompres-pdf` (poin 1 di atas) yang sama parahnya dan masih terbuka -- push ke main dengan itu belum difix berarti bug class yang sama (CSS nyangkut) masih bisa kejadian lewat jalur lain. Keputusan fix sekarang vs push dulu accepted-risk ada di user/sesi berikutnya, dicatat di sini biar gak kelewat.

**Update 27 Agustus 2026 (sesi kedua puluh):** Diminta 2 hal sekaligus: (1) buang tema "gimmicky" dark-glass `export-gambar` (retrofit visual per `design.md`, item ini sebelumnya di-plot buat setelah css-audit/konversian-breakup, tapi user eksplisit minta sekarang, di luar urutan `map.md` bagian 4 -- dicatat, bukan diam-diam diikuti), dan (2) auth consolidation: hapus gate per-modul, jadiin SATU login sebelum masuk shell (item ini SEBELUMNYA eksplisit ditandai "baru dikerjain setelah push pertama ke main" di draft `tempel-ke-map-section-4.md` -- user overrule itu secara eksplisit, jadi dikerjain, TAPI resiko yang sama yang bikin item itu ditunda tetap berlaku: belum ada bukti main branch known-good, `crud-produk`/`stok` belum pernah browser-tested, `kompres-pdf`/`crud-produk` masih punya bug CSS-leak yang belum diputusin).

**Auth consolidation — arsitektur baru:**
- `shared/auth-gate.js` (BARU): satu-satunya login gate buat SELURUH app. Dipasang oleh `router.js` SEBELUM hashchange listener dipasang / halaman manapun di-`mount()`. Reuse `.auth-gate`/`.auth-gate-box` (CSS yang udah ada di `pnm-universal.css`, dulu dipakai `dashboard`/`stok`/`konversian`) -- bukan komponen visual baru. Whitelist check (`allowed_users`, `select('email')`) jalan SATU KALI di sini, bukan per-modul.
- `app/shell.html`: `#app-nav`/`#app` di-`display:none` dari awal, baru ditampilin `router.js` setelah gate lolos (`startRouting()`).
- `app/router.js`: `routingEnabled` flag baru -- `hashchange`/render() gak ngapa-ngapain sampai `initAuthGate()`'s `onReady()` nembak. Logout (`onLoggedOut()`) nge-`unmount()` halaman aktif + sembunyiin nav/content, balik ke gate.
- **`dashboard`, `stok`, `crud-produk`, `export-gambar`**: per-page gate (2 pola beda -- `.auth-gate`/`gate-email` dipakai 2 yang pertama, `.gate-wrap`/`loginFormWrap` dipakai `crud-produk`/`export-gambar`) DIHAPUS SELURUHNYA dari `markup.js` + `index.js` masing-masing (markup gate, login handler, `checkWhitelistAndShowApp()`/setara, subscription `onAuthStateChange` lokal, `authUnsubscribe` var). Titik-titik yang dulu manggil gate lokal pas sesi expired (401 di `rpc()`/`fetch()`) sekarang manggil `window.PNMAuth.logout()` -- itu men-triggerkan `shared/auth-gate.js`'s listener global, yang otomatis nampilin gate lagi DAN (lewat `router.js`'s `onLoggedOut`) beresin halaman yang lagi ke-mount.
- **PERUBAHAN PERILAKU YANG PERLU DICATAT**: `dashboard` ASLINYA gak pernah ngecek `allowed_users` (siapapun kredensial valid = masuk). Sekarang ikut whitelist-check yang sama kayak modul lain karena satu gate = satu policy buat semua. Efek samping yang gak terhindarkan, bukan keputusan diam-diam.
- **`konversian` SENGAJA GAK DISENTUH.** Sudah dicek: pola gate-nya sama kayak `dashboard`/`stok` (`.auth-gate`), jadi SECARA TEKNIS excise-nya mirip. Tapi file ini berkali-kali ditandai di `map.md`/`map-history.md` sebagai "paling gede, paling delicate, belum full browser-tested" -- resiko nyentuh auth-critical path di file 9000+ baris tanpa akses browser buat verifikasi itu persis yang mau dihindarin. **Masih pakai gate lokalnya sendiri untuk saat ini** -- artinya: kalau user login lewat modul lain duluan lalu pindah ke `#konversian`, dia BAKAL tetap kena momen gate-nya sendiri sekilas (flash) sebelum ke-skip otomatis (karena sesi udah ada) -- konsolidasi belum penuh sampai `konversian` ikut dikerjain di sesi terpisah.

**Export-gambar visual rewrite per `design.md`:**
- `markup.js` + `style.css` DITULIS ULANG DARI NOL. Struktur baru: `.pw-topbar` (dari `nav.css`, di-load global) + `.page-head` + 2 `.card` (pilih file, hasil merge) -- SEMUA komponen (`.card`, `.field-label`/`.field-input`, `.btn-accent`/`.btn-ghost`/`.btn-primary`, `.stat-pill`) di-reuse dari `pnm-universal.css`, BUKAN token/warna baru. Ikon unicode (⏎/↗/↓) diganti Tabler (`ti ti-*`, udah global lewat `nav.js`, gak perlu di-load ulang di sini).
- `style.css` yang baru CUMA nutup yang bener-bener spesifik: list file selectable (`.eg-file-item`/`.eg-check`), chip dengan tombol hapus, frame canvas, toast. **NOL bare selector** (beda dari file lama yang jadi root cause bug sesi kesembilan belas) -- di-scope di bawah `.eg-page`/kelasnya sendiri semua.
- `index.js`'s `ensureStyle()` sekarang JUGA load `shared-pnm-universal-css` + `shared-google-fonts` (id+href sama persis kayak `dashboard`/`stok`/`crud-produk`/`konversian`), dilepas di `unmount()` dengan pola yang sama (bukan "shared selamanya"). Kode mati peninggalan tema lama (`IntersectionObserver` buat `.bezel`, `.classList.add("in-view")`) DIHAPUS -- gak ada elemen `.bezel` lagi di markup baru.

**Testing**: sama seperti sesi kedelapan belas -- gak ada akses browser/network CDN di sandbox ini. Yang dicek: `node --check` di SEMUA file yang disentuh (`shell.html` dikecualikan, bukan JS), grep manual buat mastiin nol referensi ke nama elemen/fungsi gate lama yang ketinggalan (`gateWrap`/`appWrap`/`showGate`/`authUnsubscribe`/dst) di tiap modul yang diubah, dan cross-check tiap ID yang dipakai `export-gambar/index.js` (`bulkInput`/`searchInput`/`fileList`/`selectedChips`/`finalCanvas`/`previewContainer`/`exportBtn`/`metaCount`/`previewBtn`) match persis sama markup barunya. **BELUM ADA satupun ini yang dites di browser asli** -- alur login penuh (submit form gate -> whitelist check -> redirect ke halaman default -> pindah-pindah modul -> logout -> gate muncul lagi) WAJIB ditest manual sebelum push, sama pentingnya (kalau bukan lebih) dari testing CSS-leak combo yang udah dicatat sesi sebelumnya. Auth itu security-critical path -- kalau ada yang salah di sini efeknya bisa "gak bisa masuk sama sekali" atau lebih parah "kebobolan tanpa whitelist", bukan cuma visual glitch.

**Status setelah sesi ini**: makin banyak yang nunggu verifikasi browser sebelum aman push ke main -- (a) CSS-leak `export-gambar` (sesi 18, FIXED tapi belum ditest browser), (b) CSS-leak `kompres-pdf`/`crud-produk` (sesi 18, MASIH BELUM DIFIX, user belum jawab), (c) auth consolidation di 4 modul (sesi ini, FIXED tapi belum ditest browser, security-critical), (d) `konversian` masih pakai gate lama (belum dikerjain, RESIKO KECIL karena gak diubah -- tapi berarti konsolidasi ini belum 100% "satu gate buat semua modul" sampai itu nyusul).

**Update 28 Agustus 2026 (sesi kedua puluh satu):** User lapor (screenshot) gate login sesi kedua puluh nongol TAPI SAMA SEKALI GAK KE-STYLE — form polos gaya default browser, bukan `.auth-gate`/`.auth-gate-box` card yang seharusnya.

**Root cause:** `shared/auth-gate.js` (baru dibuat sesi kedua puluh) render gate pakai class `.auth-gate`/`.auth-gate-box`/`.pr-field` dari `pnm-universal.css` -- TAPI file itu SENDIRI gak pernah nge-load `pnm-universal.css`. Sebelum konsolidasi auth, ini gak masalah: tiap halaman (dashboard/stok/dst) punya gate-nya sendiri DI DALAM markup-nya sendiri, dan halaman itu sendiri yang nge-load `pnm-universal.css` di `mount()`-nya sebelum gate-nya sendiri sempat kelihatan. Begitu gate dipindah ke level shell (sesi kedua puluh) -- muncul SEBELUM halaman manapun pernah di-`mount()` -- gak ada apapun yang sempat nge-load `pnm-universal.css` duluan. Classic "lupa dependency yang biasanya dateng gratis dari tempat lain."

**Fix**: `shared/auth-gate.js` sekarang nge-load `pnm-universal.css` + Google Fonts sendiri (id+href SAMA PERSIS kayak `SHARED_LINKS` tiap page module, biar dedup otomatis) — dipanggil di 2 tempat: (1) `initAuthGate()`, paralel sama `ensureVendorScripts()` (buat kemunculan gate pertama kali), dan (2) tiap `showGate()` dipanggil (buat kemunculan ULANG setelah logout -- defensif, karena halaman terakhir yang ke-`mount()` mungkin udah `remove()`-in link yang sama di `unmount()`-nya sendiri, pola normal tiap page module). `loadLink()`-nya dedup by id, jadi manggil berkali-kali aman/murah (no-op kalau linknya masih ada).

**Testing**: `node --check` pass, cross-check manual semua class (`.auth-gate-logo`, `.auth-gate-btn`, `.auth-gate-status`, `.pr-field`) beneran ada di `pnm-universal.css` (bukan asumsi). **Belum divalidasi browser asli** -- sandbox masih gak ada akses network. User yang lapor ini dari environment lokalnya sendiri (bukan sandbox ini), jadi laporan bug ini justru satu-satunya sinyal "browser-tested" yang ada sejauh ini buat perubahan auth sesi kedua puluh — bagus buat validasi arah, tapi belum berarti seluruh alur (whitelist check, logout->gate lagi, kombinasi pindah modul) udah lolos dites penuh.

**Update 28 Agustus 2026 (sesi kedua puluh dua):** User konfirmasi via browser asli (bukan dugaan statis lagi): CSS emang beneran nyangkut pindah dari `#kompres-pdf` ke modul lain -- persis temuan yang diflag sesi kedelapan belas tapi belum dijawab keputusannya sampai sekarang. User minta ini dibereskan biar siap push ke main.

**Fix**: `kompres-pdf/index.js`'s `unmount()` sekarang `remove()` `page-kompres-pdf-style` (sebelumnya gak pernah sama sekali -- root cause identik export-gambar, bare `*`/`html,body`/`body`/`h1`). Sekalian dibereskan juga `crud-produk/index.js`'s `page-crud-produk-style` (leak versi ringan, `table`/`th`/`td` doang) yang juga udah diflag sesi kedelapan belas tapi belum dieksekusi. Kedua fix satu baris, pola identik dengan fix export-gambar sesi kedelapan belas.

**Status CSS-leak sekarang**: SEMUA 6 modul udah bener nge-`remove()` stylesheet page-specific-nya masing-masing di `unmount()` (export-gambar, kompres-pdf, crud-produk baru; dashboard/stok gak punya style.css sendiri; konversian sudah scoped aman dari awal). Kelas bug ini kelar buat semua modul.

**Testing**: `node --check` pass buat dua-duanya. Masih sandbox tanpa browser access -- user yang test manual, dan laporan mereka barusan itu bukti browser test PERTAMA yang beneran terjadi buat sesi CSS-leak audit ini (sesi kedelapan belas s/d sekarang murni statis). Kombinasi lain (terutama yang ngelibatin auth gate baru sesi kedua puluh) masih perlu dites manual sebelum push.

**Update 28 Agustus 2026 (sesi kedua puluh tiga):** User minta pindah dari localStorage ke cookie buat penyimpanan sesi auth ("jangan khawatir soal nge-nuke sesuatu, ini sandbox" — jadi dikerjain langsung, TAPI tetap diinfoin dulu implikasinya sebelum jalan).

**Scope**: CUMA storage sesi auth (`pnm_auth_session`, dibaca/ditulis lewat `window.pnmSupabase.auth` -> `PNMAuth.*`). State UI lain yang kebetulan juga pakai localStorage (`theme`/`pnum-theme` di konversian/crud-produk/dashboard/stok, `sidebarCollapsed` & `produkView` di crud-produk, riwayat pencarian & preset di dashboard, lebar clipboard & `pic_marsup_name` di konversian, fallback riwayat di stok) SENGAJA DIBIARKAN di localStorage — itu preferensi UI per-browser, bukan "sesi", dan gak ada alasan buat ikut pindah ke cookie (cuma nambah beban tiap HTTP request tanpa manfaat).

**Implementasi**: `shared/supabase-client.js` — satu-satunya file yang disentuh (dicek dulu: gak ada file lain yang baca `localStorage` buat key `pnm_auth_session` secara langsung, semua akses emang udah lewat SDK via `PNMAuth.*`, jadi swap ini aman di-lokalisir ke 1 file). Custom storage adapter (`cookieStorage` — `getItem`/`setItem`/`removeItem` berbasis `document.cookie`) di-pass ke `auth.storage` pas `createClient(...)`, ganti default SDK (localStorage). Cookie: `Path=/`, `Max-Age=30 hari`, `SameSite=Lax`, `Secure` CUMA kalau `location.protocol === 'https:'` (biar tetap kesave pas dev lokal via http, bukan cuma di Vercel).

**Catatan jujur yang disampein ke user SEBELUM ngerjain** (bukan basa-basi nunda, cuma biar gak ada yang salah ekspektasi nanti): app ini static site tanpa backend, jadi cookie yang di-set dari sini GAK BISA httpOnly (cuma server yang bisa nyet itu) — exposure ke XSS SAMA PERSIS kayak localStorage, ini BUKAN upgrade keamanan. Beda nyata cuma 2: (1) cookie kena batas ~4KB sementara localStorage jauh lebih longgar — ditambahin `console.warn` kalau blob sesi (JSON gabungan access_token+refresh_token+user object) mendekati/lewat ~3800 byte, biar gagal-nya kelihatan bukan diam-diam; (2) cookie otomatis kekirim ke tiap request ke domain yang sama, TAPI gak ada manfaat fungsional dari itu di app ini — token ke Supabase SELALU dikirim manual lewat header `Authorization: Bearer` (beda domain: app di Vercel, Supabase di `*.supabase.co`, cookie gak nyebrang domain otomatis), jadi "auto-attach" cookie ke request API gak kepake sama sekali di sini.

**Efek samping buat sesi yang lagi aktif**: user yang udah login sebelum perubahan ini (sesi tersimpan di localStorage) GAK otomatis kebaca lagi setelah swap ke cookie — beda storage backend, gak ada migrasi otomatis. Bakal perlu login ulang sekali. Di production beneran ini perlu diumumin, tapi untuk sandbox gak masalah.

**Testing**: `node --check` (dan crude `new Function()` parse check karena file ini classic `<script>`, bukan ES module) pass. Cross-check semua modul (`konversian`/`stok`/`crud-produk`/`dashboard`) buat mastiin gak ada yang baca localStorage `pnm_auth_session` secara langsung (ketemu `AUTH_STORAGE_KEY` const di `konversian/index.js` yang cuma informational/gak dipakai). **Belum divalidasi browser asli** — login/logout/refresh-token siklus penuh, dan terutama ukuran cookie beneran vs limit 4KB browser, WAJIB dicek manual.

**Update 31 Agustus 2026 (sesi kedua puluh empat):** design.md visual retrofit steps 2-6, dimulai dari `dashboard` (satu dari dua halaman yang udah punya step 1/structural sejak sesi kelima belas -- `stok` yang satu lagi masih belum disentuh, jadi kandidat lanjutan berikutnya kalau mau lanjut thread ini).

**Scope**: HANYA `dashboard` module. Bukan retrofit ke-6 halaman sekaligus -- terlalu besar buat satu sesi tanpa browser access buat verifikasi visual tiap halaman, jadi dipecah per-modul kayak biasa.

**Pendekatan**: style.css baru (`app/pages/dashboard/style.css`), di-scope penuh di bawah `#app-root` (nol bare selector, disiplin sama kayak konversian), di-load/dilepas pola sama kayak halaman lain (ensureStyle()/unmount()). Ini OVERRIDE terhadap class dashboard-only di `pnm-universal.css` (`.stat-card.blue` dkk, `.chip-yes/no`, `.badge-instrument/set/unit`, `.days-kritis/warn/aman`) -- BUKAN edit ke `pnm-universal.css` itu sendiri, jadi konsumen lain file itu (termasuk legacy `dashboard.html` standalone kalau masih ada di repo asli -- gak ada di sandbox upload ini) tetap gak kesentuh.

**Trik yang dipake**: `#app-root .stat-card::before{content:none}` dan `#app-root .stat-icon{...}` menang lawan `.stat-card.blue::before`/`.stat-icon.blue` murni dari specificity (1 id + 1 class ngalahin 2 class), jadi warna-per-item stat card ilang TANPA perlu ubah markup/JS-nya sama sekali (class `blue`/`green`/dst masih ada di kode, cuma udah gak ngaruh apa-apa lagi secara visual -- dicek sengaja dibiarin gitu, bukan dilupa).

**Klasifikasi status vs kategori**: `.chip`/`.days-pill` (Ada/Belum buat harga-link_v6-AKD, level risiko stok) itu genuine status -> jadi dot+label (dot 6px, warna dari `--success`/`--warning`/`--danger` sesuai kondisi). `.badge` (Tipe/Entitas/Channel) itu label kategori, bukan status (gak ada sumbu baik/buruk) -> jadi neutral bordered pill tanpa dot, biar gak salah nyiratin makna good/bad yang gak ada.

**Perubahan lain**: `markup.js` -- dicopot semua inline `style="color:var(--success/accent/warning/purple)"` di icon `prog-card-title`/`insight-card-title` (7 titik) biar konsisten neutral, gak bisa di-override lewat CSS class karena inline style menang specificity. `index.js` -- kolom "Order Terakhir" (tanggal) di tabel Populasi ketauan belum mono waktu ngaudit step 6, ditambahin `font-family:var(--font-mono)`; sisanya (kode produk, harga, qty, no. AKD) udah mono dari awal, dicek lewat grep dulu sebelum nyimpulkan gak ada gap lain.

**SENGAJA belum disentuh**: chart SVG (trend bar/line/donut/wordtree/forecast) yang gambar warna semantik per-seri (`var(--accent)`/`var(--success)`/dst) langsung di `index.js` -- itu bukan anti-pattern "beda warna per kartu" yang dimaksud design.md, jadi dibiarin daripada nebak-nebak retrofit tanpa bisa verifikasi visual. `stok` module (satu lagi yang udah step 1) belum dikerjain sesi ini -- kandidat sesi berikutnya kalau mau lanjut thread ini. Charts/color pada 4 modul lain yang belum step 1 sama sekali (konversian/crud-produk/kompres-pdf/export-gambar) juga di luar scope sesi ini.

**Testing**: `node --check` pass buat `index.js`/`markup.js`, brace-balance manual buat `style.css` baru. Masih sandbox tanpa browser access (caveat yang sama terus berulang di file ini) -- retrofit warna/dot/spacing ini PALING butuh verifikasi visual asli sebelum dianggap beres, belum ada satupun di sesi ini.


**Update 31 Agustus 2026 (sesi kedua puluh lima):** Lanjutan langsung dari sesi kedua puluh empat -- `stok` module dapet retrofit yang sama (design.md steps 2-6), sekaligus koreksi satu kesalahan dari sesi sebelumnya.

**Koreksi ke dashboard/style.css (sesi sebelumnya)**: dot warna status pake `var(--success)`/`var(--warning)`/`var(--danger)` (token LAMA pnm-universal.css), padahal design.md's Color tokens section eksplisit bilang halaman yang diedit HARUS pake token barunya persis (`--success-dot`/`--warning-dot`/`--danger-dot`, hex beda dari yang lama) -- biar semua halaman yang di-retrofit convergent duluan sebelum eventual merge ke satu token set. Fixed: 3 custom property itu sekarang didefinisikan lokal di bawah `#app-root` (bukan `:root` global -- aman dari bug token-leak yang sama yang diflag komentar nav.css) di kedua file (`dashboard/style.css` DAN `stok/style.css` baru, sejak awal).

**stok ternyata beda kondisi dari dashboard**: lebih dekat ke compliant dari awal (soft-tint bukan solid-fill kayak `.stat-card.blue` dashboard), TAPI nemu 2 anti-pattern yang gak ada di dashboard:
1. `.card{box-shadow:var(--shadow)}` -- shadow, yang design.md eksplisit larang ("No shadows, no blur, no gradients, no glass"). Dihapus.
2. `.icon-box` 42px/11px-radius, lebih gede dari spec design.md (32px/7px) -- di-resize. `.metric-icon` udah kebetulan 32px, cuma radius (9px->7px) + warna yang perlu dibenerin.

**Spacing juga diaudit**: `.card` padding 22px/24px -> 16px, `.card-head` margin 18px -> 16px, `.stat-row` gap 10px -> 12px, `.summary-row` gap 20px -> 16px, `.metric-group` gap 22px -> 16px, `.dropzone` padding 36px -> 32px. Semua sebelumnya ad-hoc px yang gak ada di skala 8pt design.md.

**Status jadi dot+label**: `stat-pill`(precheck total/match/skip)/`metric`(Last Sync updated/skipped)/`act-num`(tabel Recent Activity) itu 3 komponen beda buat 2 state yang sama (netral/ok/warn) -- ketiganya di-flatten ke text netral + dot 6px, background/border tint dicopot. `skip-panel` (banner warning full-tint) juga diratain ke card netral biasa + satu dot di title-nya, bukan seluruh panel diwarnain.

**Keputusan yang diflag, bukan diputuskan sepihak**: `.dropzone-icon` (52px, ikon hero drop-target) SENGAJA gak di-resize ke 32px kayak `.icon-box`/`.metric-icon` -- itu bukan komponen "module icon square" yang dimaksud spec Iconography, dan tint accent-nya CUMA dipertahankan pas state `.dropzone.drag` aktif (bukan permanen) -- true judgment call, ditulis di komentar style.css biar kelihatan sebagai keputusan sadar, bukan kelewatan.

**Testing**: `node --check` pass buat `stok/index.js`+`markup.js`, brace-balance manual buat kedua `style.css` (dashboard yang dikoreksi + stok yang baru). Caveat sama yang berulang terus: nol browser access di sandbox ini, retrofit warna/spacing/dot ini paling butuh mata asli sebelum dianggap beres.

**Sisa thread ini**: 4 modul lagi (`konversian`/`crud-produk`/`kompres-pdf`/`export-gambar`) belum step 1 sama sekali, jadi steps 2-6 buat mereka nunggu step 1 duluan -- beda urutan kerja dari `dashboard`/`stok` yang tinggal lanjut steps 2-6 doang.


**Update 31 Agustus 2026 (sesi kedua puluh enam):** Lanjutan dari sesi kedua puluh lima. Target awal thread ini (4 modul belum step 1: konversian/crud-produk/kompres-pdf/export-gambar) ternyata cuma 2 yang beneran belum tersentuh -- `export-gambar` udah kepalsu-selesai dari sesi kesembilan belas (dicek langsung sebelum mulai kerja, bukan asumsi dari catatan lama). Jadi target sesi ini `kompres-pdf` -- dan ternyata beda kelas kerja dari dashboard/stok, bukan cuma override CSS.

**Kenapa full rewrite, bukan override tipis**: `kompres-pdf` adalah alat client-side murni (gak pernah butuh `pnm-universal.css`/auth) dengan token system sendiri yang PERSIS dari `kompres-pdf.html` lama, gak pernah disentuh sejak migrasi SPA. Ketauan 3 pelanggaran design.md yang gak bisa dibenerin lewat override doang: (1) `body{background:radial-gradient(...)}` -- gradient, dilarang total; (2) `.card`/`.tab.active` pakai `box-shadow` -- dilarang; (3) nol dark-mode support (satu blok `:root` doang), bakal kelihatan patah begitu sidebar nav global (yang sekarang always-on di semua halaman) dipake toggle dark mode.

User dikonfirmasi eksplisit mau full rewrite (bukan skip / bukan topbar-doang) lewat pilihan di elicitation.

**Pendekatan yang dipilih (beda dari export-gambar punya)**: export-gambar (sesi 19) ngadopsi HABIS-HABISAN class pnm-universal.css (`.card`/`.btn`/`.field-input` dst), style.css sisa cuma ~85 baris. `kompres-pdf` SENGAJA TIDAK ngikutin pola itu -- alasannya index.js+markup.js gabungan >1400 baris, dan class-class lama (`.tab`, `.segmented`, `.gauge`, `.chip`, `.badge`, `.card`, `.btn`) dirujuk puluhan kali lewat `classList.toggle()`/`querySelector()` di seluruh index.js. Ganti nama class di semua titik itu tanpa browser buat verifikasi drag-drop/tab-switch/compress-split-merge flow adalah risiko yang gak sepadan. Solusi yang dipakai: KEEP semua nama class persis sama, cuma di-SCOPE di bawah `.kp-page` (element classList container dapetin, ganti dari `.wrap` lama) -- termasuk yang sekarang jadi penting untuk discope (`.card`/`.btn`/`.chip`/`.hidden`/`.field`, karena pnm-universal.css yang BARU dimuat sesi ini definisiin ulang nama sama itu secara global juga; spesifisitas `.kp-page .card` otomatis menang lawan `.card` bare punya pnm-universal, gak butuh `!important`).

**Yang diubah konkretnya**: `:root` -> `.kp-page{...}` (nama custom property SAMA -- `--bg`/`--ink`/`--accent` dst -- cuma di-scope + nilai diganti token design.md yang persis, plus varian `html[data-theme="dark"] .kp-page{...}` baru yang sebelumnya gak ada sama sekali). Semua `box-shadow` dicopot. Gradient body dicopot jadi flat `--bg`. Padding body (`48px 20px 80px`, bare/global) pindah ke div baru `.kp-page .kp-main` (SENGAJA bukan elemen `<main>` -- pnm-universal.css punya rule `body:has(.dropzone) main{...}` yang di-scope buat stok, dihindari daripada ngandelin itung-itungan spesifisitas). `pw-topbar` breadcrumb ditambah (step 1, pola sama kayak dashboard/stok/export-gambar). 3 SVG dropzone yang hardcode `stroke="#6c685f"` (warna palet lama) diganti `stroke="currentColor"` -- semua icon lain di index.js UDAH currentColor dari awal, dicek lewat grep sebelum nyimpulkan cuma 3 titik ini. `.badge` status file (Menunggu/Memproses/Selesai/Gagal) yang tadinya full-tint pill diratain jadi dot+label, sama pola persis kayak dashboard/stok, pake token `--success-dot`/`--warning-dot`/`--danger-dot` design.md yang didefinisikan lokal (bukan `:root` global).

**SENGAJA belum disentuh**: icon library TETAP inline SVG, TIDAK di-swap ke Tabler kayak export-gambar -- alasannya risiko (25+ titik SVG across dua file, gak ada browser buat cek ukuran/alignment abis diganti) lebih besar dari manfaat "satu icon library" doang, sedangkan anti-pattern yang design.md eksplisit larang (gradient/shadow/warna beku) semuanya udah kebenerin.

**Testing**: `node --check` pass buat `index.js`+`markup.js`. Brace-balance manual pass buat `style.css` (123/123). Div-balance kompres-pdf's markup.js SEMPAT kelihatan off-by-one lewat naive regex, ternyata false-positive dari komentar JS di kepala file yang literally nulis `<div class="kp-main">` sebagai teks dokumentasi (bukan markup beneran) -- dicek ulang pake html.parser Python buat mastiin, false alarm. Caveat yang sama terus berulang: NOL browser access di sandbox ini -- retrofit sebesar ini (full rewrite, bukan override kecil) PALING butuh mata asli sebelum dianggap beres, apalagi drag-drop/tab-switching/compress-split-merge flow yang gak pernah dicoba jalan beneran sesi ini.

**Sisa thread ini**: `konversian` (9000+ baris index.js, modul paling gede) dan `crud-produk` (yang juga masih ada dobel-sidebar belum diputusin) yang beneran belum step 1 sama sekali.


**Update 1 September 2026 (sesi lanjutan, setelah user tes beneran di browser):** User kirim screenshot `kompres-pdf` -- STYLING KEBUKTI JALAN (topbar/tabs/dropzone semua kelihatan flat/neutral sesuai design.md, full rewrite sesi sebelumnya sukses secara visual). TAPI juga nemu 1 bug (`initAuth is not defined`, bikin `dashboard` gak bisa mount SAMA SEKALI) dan minta 2 hal baru: (1) fix bug itu, (2) redesign nav -- crud-produk's dobel sidebar dibenerin dengan mindahin semua sub-navigasi modul ke accordion di GLOBAL sidebar, plus search bar nav yang SEBELUMNYA cuma `<div>` dekoratif (non-fungsional) jadi beneran nyari.

**Bug initAuth (dashboard)**: `dashboard/index.js` punya `initAuth();` dipanggil di baris terakhir `mount()`, TAPI gak ada fungsi bernama itu di file itu SAMA SEKALI -- dashboard udah pakai `window.PNMAuth` langsung di semua tempat lain. Ini bukan regresi dari sesi manapun punya saya (dicek dulu, gak pernah nyentuh baris itu) -- kemungkinan sisa refactor auth dari sesi sebelum sesi kedua puluh empat yang gak sempet dibersihin. Dihapus, satu baris, dashboard sekarang bisa mount.

**Nav redesign -- scope "whole system" (user pilih ini, bukan pilot crud-produk doang)**:
1. **Registry pattern baru**: tiap modul yang punya sub-halaman dapet `subnav.js` sendiri (`crud-produk/subnav.js`: produk/set/akd/kfa/bulk; `konversian/subnav.js`: cari/sesi/riwayat/converter/setcari/dictionary -- LIST INI DICEK LANGSUNG DARI KODE konversian, bukan tebakan dari contoh user, ternyata persis cocok). File-file ini ringan/tanpa logic berat, di-import EAGER sama `nav-markup.js` (beda dari `index.js` tiap modul yang lazy-loaded router.js) karena sidebar butuh data ini SEBELUM user pernah buka modulnya.
2. **`nav-markup.js`/`nav.css`/`nav.js` ditulis ulang**: accordion sub-item cuma muncul di DOM buat modul yang lagi aktif (gak ada state expand/collapse terpisah yang perlu di-track -- murni derived dari route). Search bar LAMA (div dekoratif, tombol `/` gak ngapa-ngapain) diganti `<input>` beneran + dropdown hasil + keyboard nav (arrow/enter/escape) + index gabungan modul+subnav, dibangun dari `buildSearchIndex()` di `nav-markup.js`.
3. **`router.js`**: hash format sekarang `#module` ATAU `#module/sub` (misal `#crud-produk/kfa`), bikin sub-halaman jadi bookmark-able/refresh-safe/back-button-safe, yang SEBELUMNYA cuma state JS di dalam halaman doang. Ada FAST PATH: kalau modul sama tapi sub beda, panggil `page.setSubroute(sub)` (kalau modul itu expose fungsi itu) alih-alih remount penuh -- biar klik antar-tab TETEP instant kayak sebelumnya, gak jadi re-fetch dari Supabase tiap klik.
4. **`crud-produk`**: `<aside class="sidebar">` DIHAPUS TOTAL dari markup.js (brand/collapse-btn/5 tombol data-view/Log Aktivitas/user-email/logout) -- INI bug "dobel nav" yang sebenernya, sekarang cuma 1 sidebar (global). "Log Aktivitas" dipindah ke topbar (masih action asli, bukan navigasi, jadi gak masuk accordion). Brand/user-email/logout DIHAPUS (bukan dipindah) karena udah duplikat 100% sama apa yang global nav tampilin. `switchView()` di index.js LOGIC-nya gak berubah, cuma DIPANGGIL BEDA (`setSubroute()` dari router, bukan lagi listener di tombol sendiri yang udah gak ada). Fix pas ngedit: sempet ke-delete gak sengaja `loadProduk()`/`refreshProdukFilterCounts()`/`handleNavParamsIfAny()` (panggilan awal mount, ke-include gak sengaja pas hapus blok lain) -- ketauan lewat grep sebelum lanjut, langsung dipulihin.
5. **`konversian`**: LEBIH HATI-HATI -- file terbesar/paling delicate (9000+ baris). Baris subtab bawaan halaman ini (`subtabCari.addEventListener` dkk) SENGAJA DIBIARIN, gak dihapus -- dobel secara visual sama accordion baru tapi TIDAK RUSAK/tidak saling ganggu (`setSubroute()` yang baru cuma manggil `switchSubTab()` yang sama yang tombol lama juga panggil). Ini keputusan sadar buat batesin risiko di file paling riskan, dicatat eksplisit di `subnav.js`-nya biar sesi depan tau ini "belum", bukan "lupa".

**SENGAJA belum disentuh**: `home/index.js`'s halaman/kartu (kalau masih ada) gak diapa-apain -- di luar scope. crud-produk's own "Spotlight ⌘K" (cari PRODUK, beda dari search nav yang baru, yang cari NAVIGASI) TETAP terpisah, sengaja gak digabung -- dua hal yang beda secara konseptual.

**Testing**: `node --check` pass semua file JS yang disentuh (router.js, nav.js, nav-markup.js, kedua subnav.js, crud-produk/index.js+markup.js, konversian/index.js, dashboard/index.js). Brace-balance manual buat nav.css. Div-balance HTML-parser check buat crud-produk/markup.js (bersih). Caveat SAMA yang terus berulang tapi PALING PENTING buat sesi ini: NOL kali dicoba di browser beneran -- accordion, search dropdown+keyboard-nav, fast-path routing (setSubroute vs remount), semua BELUM PERNAH divalidasi kerja beneran. Ini perubahan STRUKTURAL ke router inti + 2 modul yang paling sering dipake (konversian/crud-produk) -- prioritas paling tinggi buat dites browser beneran sebelum dipercaya, lebih tinggi dari retrofit visual steps 2-6 yang masih di roadmap.


**Koreksi kecil (VS Code TS language server nangkep sebelum sempet ke-test browser):** `crud-produk/index.js`'s `export function setSubroute` di sesi lanjutan di atas ke-taro NESTED di dalam `mount()`'s body -- `export` cuma valid di top-level module, itu SyntaxError beneran di browser meskipun `node --check` (tanpa `package.json` `"type":"module"`, jadi dia parse sebagai CommonJS longgar, bukan ESM strict) gak nangkep. Fixed: `switchView` TETEP nested di dalam mount() (gak dipindah, biar closure atas elemen DOM yang di-cache pas mount() gak keganggu), tapi sekarang di-assign ke `_switchView` (variable module-level beneran) yang `setSubroute` (juga top-level beneran sekarang) panggil lewat situ. `konversian`'s `setSubroute` DICEK ULANG -- itu udah bener dari awal (ke-taro SETELAH `mount()`'s closing brace, bukan di dalam), gak kena bug yang sama.

Verifikasi kali ini dinaikin: SEMUA 15 file JS yang pernah disentuh sesi-sesi lanjutan (bukan cuma yang baru) di-check ulang dengan cara yang lebih ketat -- disalin sementara ke `.mjs` (maksa Node parse sebagai ESM beneran, bukan CommonJS longgar) -- semua lolos. `node --check` biasa TIDAK CUKUP buat validasi export/import placement di codebase zero-build kayak ini; cek `.mjs` di atas seharusnya jadi standar buat sesi-sesi berikutnya juga.


**4 bug ditemukan lewat testing browser beneran (setelah koreksi setSubroute crud-produk di atas), diperbaiki sesi ini:**

1. **`konversian`'s subnav gak jalan**: `ReferenceError: switchSubTab is not defined at Module.setSubroute`. Root cause SAMA PERSIS kayak crud-produk's `setSubroute` bug sebelumnya, cuma manifestasinya beda -- `export function setSubroute` udah bener DI TOP-LEVEL (bukan nested, itu udah dicek benar), TAPI badan fungsinya manggil `switchSubTab`, yang nested DI DALAM `mount()` dan gak bisa diakses dari luar. Fixed pake pola indirection yang sama (`_switchSubTab`, module-level, di-assign begitu `switchSubTab` didefinisiin di dalam mount(), dipanggil dari `setSubroute` yang beneran top-level).

2. **crud-produk's `Cannot access 'lastQuery' before initialization`**: koreksi sesi sebelumnya (mindahin `loadProduk()`/`refreshProdukFilterCounts()`/`handleNavParamsIfAny()` balik ke posisi awal, dekat bagian auth) taro pemanggilan itu SEBELUM `let lastQuery = ''` sempet jalan (deklarasinya ratusan baris di bawah) -- `loadProduk()` baca/tulis `lastQuery`, kena temporal dead zone. Dipindah ke UJUNG `mount()` (sebelum `switchView(initialSub)` yang udah ada di situ) -- satu-satunya titik yang dijamin aman dari TDZ apa pun tanpa nelusurin semua dependency satu-satu.

3. **`auth-session.js`/`toast.js` "Identifier ... has already been declared"**: BUKAN bug dari sesi manapun saya -- pre-existing, `konversian` muat `/shared/auth-session.js?v=20260812a` & `/shared/toast.js?v=20260813a` (dengan cache-busting), sedangkan 4 modul lain (dashboard/stok/export-gambar/crud-produk) muat file YANG SAMA PERSIS tanpa suffix versi. Selama navigasi antar-modul selalu full page reload (`window.location.href`, cara lama), ini gak pernah ketauan -- reload reset semua state JS. Nav redesign bikin navigasi jadi SPA murni (gak reload), jadi `loadScript()`'s guard (match STRING PERSIS) gagal ngenalin dua src yang beda string tapi sama filenya, keduanya ke-load, `const PNMAuth`/`const PNMToast` di-declare dua kali. Fixed: suffix `?v=...` DIHAPUS dari 3 URL di `konversian` (toast.js/supabase-client.js/auth-session.js) biar match konvensi mayoritas (bare URL) yang 4 modul lain udah pakai -- fix yang lebih kecil daripada nambahin versi ke 4 file.

4. **`export-gambar`'s file-list crash** (`Cannot read properties of null` di `renderFileList`/`toast`): juga PRE-EXISTING, bukan dari sesi manapun saya. `loadFileList()` fetch ke Supabase Storage tanpa nunggu (fire-and-forget dari `mount()`), TANPA guard "masih ke-mount gak" sebelum nulis ke DOM pas fetch-nya kelar. Kalau user pindah halaman SEBELUM fetch itu kelar (gampang kejadian sekarang, navigasi udah instant lewat nav baru, beda dari dulu yang full reload), `container.querySelector('#searchInput')` balikin null karena `container`'s isinya udah diganti markup halaman lain. Fixed: tambah flag `isMounted` (true di mount(), false di unmount()), dicek di 3 titik lanjutan-setelah-`await` di `loadFileList()` sebelum nulis DOM apa pun.

**PENTING -- diflag, BUKAN diaudit/difix sepenuhnya sesi ini**: dicek, TIDAK ADA satupun modul (crud-produk/konversian/dashboard/stok/export-gambar) yang punya konvensi "masih ke-mount gak" sebelum sesi ini -- `mountedContainer` di-track di semua tempat tapi gak pernah dipakai sebagai guard di mana pun. Bug class #4 di atas (async fetch nulis DOM tanpa cek unmount) SANGAT MUNGKIN ada di modul lain juga (loadProduk/loadSetList/loadAkd/loadKfa di crud-produk; loadSesiList/loadRiwayatList/loadDictionary di konversian; dst) -- TIDAK diaudit/difix satu-satu sesi ini, itu kerjaan terpisah yang lebih besar. Sekarang jauh lebih mungkin ke-trigger karena navigasi antar-modul udah instant (dulu ke-mask sama full page reload). Kandidat kuat buat sesi dedicated berikutnya.

**Verifikasi**: SEMUA file yang disentuh (termasuk export-gambar yang baru kena sesi ini) di-cek ulang via `.mjs` (ESM strict) -- 10 file, semua lolos. `konversian`'s versioned URL dipastikan gak ada sisa (grep bersih, cuma nongol di komentar penjelasan).


**MILESTONE -- migrasi SPA 100% selesai secara struktural (2 September 2026):** User konfirmasi langsung: "its deleted, i tested it and now working fine", plus `tree /f` yang nunjukkin root sekarang cuma `index.html` (shell) -- semua 6 legacy `.html` (`crud-produk.html`/`dashboard.html`/`stok.html`/`konversian.html`/`kompres-pdf.html`/`export-gambar-gabungan.html`) sudah dihapus. Nav redesign (accordion sidebar + search + deep-linking) DAN kelima bug yang ketemu lewat testing browser beneran (initAuth dashboard, setSubroute konversian+crud-produk x2, lastQuery TDZ, vendor-script version mismatch, export-gambar stale-closure crash) semua dikonfirmasi jalan normal sekarang.

Checklist "sebelum push ke main" (bagian 5 di map.md) SEKARANG SEMUA CENTANG kecuali item roadmap "setelah push" (retrofit visual steps 2-6 buat konversian/crud-produk, css-audit.md, breakup konversian/index.js, audit async-abis-unmount). `konversian` masih pakai gate lokalnya sendiri (belum ikut konsolidasi shared-gate) TAPI dikonfirmasi jalan normal, jadi diturunin dari "blocker" ke "nice-to-have nyusul".

**Sesi kedua puluh tiga — mulai retrofit visual `design.md` (konversian & crud-produk), item pertama roadmap "setelah push":** User minta lanjutin kerjaan, dikasih 4 pilihan item roadmap (bagian 5 map.md), pilih retrofit visual duluan. Audit dulu sebelum nulis kode -- baca `design.md` penuh, lalu bandingin ke section KONVERSIAN (baris 551-1304) dan CRUD PRODUK (baris 1304-1698) di `pnm-universal.css`.

Temuan penting yang GAK kesebut di map.md versi lama: langkah 3 (icon library) dan langkah 6 (angka pakai monospace) dari checklist migrasi `design.md` TERNYATA SUDAH BERES buat kedua modul dari sesi-sesi sebelumnya -- Tabler icons webfont udah di-load (`crud-produk/index.js`), dan semua kode/harga/qty/tanggal (pc-kode, sl-kode, cr-kode, cr-qty, ar-no, log-table, dst) udah pakai `var(--font-mono)`. Jadi checklist 6 langkah itu BUKAN 0% buat kedua modul ini kayak yang map.md lama implikasikan -- udah 3 dari 6 (langkah 1/3/6) beres duluan.

Langkah 2 (swap warna hardcode -> token netral) juga TERNYATA sebagian udah jalan -- kedua modul udah pakai `var(--text)`/`var(--border)`/dst dari `pnm-universal.css`, BUKAN hex hardcode per-modul. Tapi masih ada pelanggaran nyata: `box-shadow` dekoratif di banyak komponen (`.prod-card` hover, `.gate-card`, `.confirm-card`, `.cmdk-box`, `.add-filter-pop`, `.theme-switch .knob` di crud-produk; `.auth-gate-box`, `.user-menu-dropdown`, `#autocomplete`, `.rcard:hover`, `.pr-tab.active`, `.com-slider`, `.toast`, `.btn-record`, `.modal-box`, `.pref-switch .knob` di konversian) dan hover yang ganti warna border ke `--accent` + `translateY(-3px)` di `.prod-card` -- keduanya eksplisit dilarang `design.md` ("No shadows...flat surfaces + hairline borders only", hover cuma `border-strong` + max `translateY(-1px)`, no color shift). SEMUA itu dibersihin sesi ini, CSS-only, murni hapus/ganti properti -- gak nyentuh HTML/markup/JS sama sekali jadi resiko break-nya rendah. Focus-ring `box-shadow` (`#search-input:focus`, `.conv-manual-input:focus`, keyframe `rt-field-pulse`) SENGAJA dibiarin -- itu bukan depth-shadow/anti-pattern, itu aksesibilitas.

**Hati-hati diambil**: ada `.auth-gate-box` identik teksnya di 3 tempat (dashboard baris 298, konversian baris 557, stok baris 1919 -- masing-masing modul punya definisi sendiri walau sama persis). Cuma versi konversian (557) yang diedit pakai `sed` baris-spesifik (bukan str_replace berbasis teks karena ada duplikat), dashboard & stok SENGAJA gak disentuh -- di luar scope sesi ini (cuma diminta konversian & crud-produk), dan keduanya udah 'selesai' status retrofitnya di sesi lain jadi gak mau resiko nyenggol tanpa alasan yang diminta.

Langkah 4 (status pill berwarna -> pola titik+label) BELUM disentuh -- ini item TERBESAR yang tersisa. Beda karakter dari kerjaan CSS-only di atas: markup badge-nya (`.seal-*`, `.status-pill.*`, `.filter-chip.active`, `.log-action.*`, `.row-status.*`, `.adv-filter-chip`) di-generate dari JS di `index.js` (3137 baris crud-produk, 9114 baris konversian), belum diaudit titik generate-nya satu-satu. Sengaja gak dikerjain sesi ini -- resikonya beda kelas (nyentuh JS render logic, bukan cuma CSS), WAJIB browser-test beneran sebelum dianggap selesai, cocoknya jadi sesi terpisah yang fokus di situ aja.

Langkah 5 (audit spacing ke skala 8pt) diturunin prioritas -- dicek, ini utang lama yang ADA DI SEMUA MODUL (termasuk yang udah 'selesai' kayak dashboard/stok), bukan spesifik konversian/crud-produk, dan normalisasi borongan ke skala 8pt resikonya tinggi (banyak layout dense yang px-nya udah di-tuning manual buat pas). Diflag di map.md, gak dieksekusi.

**Verifikasi sesi ini**: brace count `pnm-universal.css` sebelum/sesudah edit seimbang (1450/1450), grep ulang section konversian+crud-produk mastiin cuma sisa `box-shadow` yang emang sengaja dipertahanin (focus-ring + satu `box-shadow:none` yang udah jadi no-op harmless). TAPI -- ini SEMUA verifikasi statis/grep, BUKAN browser real. Belum ada konfirmasi visual dari user kalau perubahan ini beneran kelihatan benar di browser (border/hover/shadow-removal bisa aja kelihatan aneh di komponen tertentu yang gak kepikiran pas audit). Jangan anggap langkah 2 checklist "selesai penuh" sebelum user konfirmasi lewat testing browser beneran, sama kayak konvensi sesi-sesi migrasi sebelumnya.


**Sesi kedua puluh empat -- audit konsistensi visual shell (breadcrumb, duplikasi kontrol, bug nav search), BUKAN lanjutan retrofit design.md:** User kasih 5 screenshot + brief audit lengkap dengan format P0/P1/P2 eksplisit, minta audit-dulu-baru-fix, dan eksplisit larang redo kerjaan yang udah 'selesai' (stok's shadow/color retrofit, kompres-pdf's design.md rewrite) -- scope-nya murni shell-level polish.

**Bug #1 -- nav search "cascading text":** User laporan visual (screenshot: teks module list dan search-result dropdown keliatan numpuk/blend jadi satu). Ditelusuri kode `.pw-nav-search-results`/`nav.js`/`nav-markup.js` cukup dalam -- posisi dropdown (`position:absolute;top:calc(100% + 4px)` relatif ke `.pw-nav-search-wrap`) emang PAS di atas `.pw-nav-eyebrow`+`.pw-nav-list`, jadi secara SPASIAL memang overlap itu 'by design' (dropdown ngambang di atas list). Root cause KENAPA background-nya kelihatan gak nutup sempurna gak bisa dipastikan 100% cuma dari baca kode (custom-property scoping ke `.pw-nav`/`.pw-topbar` udah bener, z-index/stacking context juga keliatan bener secara spec) -- kemungkinan besar tetep soal opacity/stacking yang cuma kekonfirmasi kalau dirender beneran. Fix yang diambil: DEFENSIF, nutup semua kemungkinan sekaligus, bukan cuma 1 fix yang 'diyakini' tepat -- (a) list disembunyikan `visibility:hidden` (bukan `display:none`, biar height sidebar gak reflow) via class baru `.pw-nav--searching` yang di-toggle `renderSearchResults()`/`hideSearchResults()` di `nav.js`, (b) `.pw-nav-search-results` dikasih `background: var(--surface-raised, #ffffff)` (fallback eksplisit) + `isolation:isolate` (maksa stacking context sendiri, gak gantung ke ancestor manapun). Efeknya: SEKARANG WALAUPUN background si dropdown somehow gak opaque sempurna, gak ada apa pun di baliknya buat nge-blend karena list-nya udah disembunyikan duluan. **BELUM diverifikasi browser** -- ini strategi "fix semua kemungkinan penyebab" karena gak ada cara mastiin penyebab pastinya tanpa render beneran, ditulis eksplisit ke user biar gak dianggap udah confirmed-fixed.

**Item #2 -- breadcrumb "Workspaces / X" redundan:** User cuma nanya soal `stok` (screenshot Stock Inventory nunjukkin "Workspaces / Stock Inventory"), tapi grep `pw-topbar-crumb` nemu POLA YANG SAMA PERSIS di 4 halaman: `stok`, `dashboard`, `kompres-pdf`, `export-gambar` (semua pakai class `.pw-topbar-crumb` dari shared shell). Diputuskan fix SEMUA 4, bukan cuma stok -- kalau cuma stok yang dibenerin, itu malah BIKIN inkonsistensi baru (3 halaman lain masih nunjukkin "Workspaces /", 1 halaman enggak). Prefix "Workspaces / " dihapus, cuma nama modul (bold) yang disisain. Helper `topbarMarkup()` di `nav-markup.js` (dicek: BELUM ada pemanggilnya di kode manapun sekarang, exported buat halaman masa depan) disamain juga biar halaman baru yang pakai helper ini otomatis konsisten. `crud-produk` dan `konversian` gak punya `.pw-topbar-crumb` sama sekali (liat item #3 di bawah), jadi gak ada yang perlu diubah di situ buat item ini.

**Item #3 -- CRUD Produk topbar & duplikasi:** Ditelusuri `crud-produk/markup.js` dari atas -- komentar existing di file itu sendiri udah bilang sidebar internal-nya (`<aside class="sidebar">`, brand/collapse/5-nav-button/logout) UDAH dihapus sesi sebelumnya ('double nav' fix), Log Aktivitas UDAH dipindah ke topbar-actions. Tapi ketemu SATU yang kelewat: `<div class="theme-switch" id="themeToggle">` masih ada di topbar internal crud-produk, DUPLIKAT sama toggle di sidebar global (`.pw-nav-theme-toggle`). Dicek persis bug class yang sama kayak yang UDAH dibenerin di `stok` (komentar di `stok/markup.js` eksplisit bilang `#theme-toggle-gate`/`#theme-toggle-app` 'dihapus entirely -- shared nav now owns theming') -- pola yang SAMA, cuma crud-produk kelewat pas sesi itu. Sebelum hapus, dicek DULU `index.js`-nya: `themeToggle`/`themeIcon` di-`getElementById` TANPA null-check (`themeToggle.addEventListener(...)` bakal throw kalau elemen gak ada) -- jadi hapus markup doang TANPA hapus JS-nya bakal bikin index.js crash pas load. Blok JS-nya (const themeToggle/themeIcon, function applyTheme, click listener) dihapus bareng markup-nya, TAPI IIFE inline yang set `data-theme` awal (sebelum external script kelar load, biar gak ada flash-of-wrong-theme) SENGAJA DIBIARIN -- itu bukan bagian dari tombol, itu inisialisasi tema halaman. Dicek localStorage key ('theme') dan attribute (`html[data-theme]`) yang dipakai toggle sidebar global (`nav.js`) SAMA PERSIS sama yang dibaca index.js crud-produk -- jadi aman, gak butuh perubahan lain.

**SENGAJA GAK disentuh, dicek dulu alasannya sebelum diputuskan skip:**
- **Konversian punya bug #3 yang SAMA** (`#theme-toggle` duplikat) TAPI beda karakter risikonya: ada baris LAIN di file yang sama (`document.getElementById('theme-toggle').click()`, dipakai fitur panel preferensi buat 'reuse logic toggle yang udah ada' -- komentar aslinya bilang gitu) yang SIMULASIKAN klik ke tombol itu. Kalau tombolnya dihapus tanpa nelusurin fitur preferensi itu juga, `.click()` ke elemen null bakal throw. Beda dari crud-produk yang cuma 1 titik pemakaian bersih -- ini butuh sesi terpisah yang fokus nelusurin dependency itu, gak masuk 'targeted polish'.
- **Crud-produk masih BELUM punya `.pw-topbar` sama sekali** -- topbar internal-nya sendiri (search+spotlight+view-toggle+log+tambah-produk) yang sekarang JADI satu-satunya topbar halaman ini. Nambahin `.pw-topbar` di atasnya berarti DUA topbar numpuk kecuali topbar internal-nya dirapiin/dipendekin dulu -- itu redesign struktural, bukan polish kecil, di luar scope sesi ini. Didokumentasikan sebagai gap buat sesi terpisah.
- kompres-pdf's hero header (eyebrow+h1+sub) dan layout centered-nya -- dicek `style.css`-nya, SUDAH full rewrite design.md (token PERSIS sama kayak design.md, shadow/gradient udah dicopot semua, dikonfirmasi browser-tested sesi lain per komentar di file). 'Kesan standalone tool' itu emang karena konten/fungsinya beda (satu alat vs modul data-dense), bukan pelanggaran design language -- gak diapa-apain selain breadcrumb.
- `.card`/`.auth-gate-box` di STOK section pnm-universal.css masih pakai `box-shadow:var(--shadow)` -- KETEMU pas audit, TAPI eksplisit di luar scope (user bilang 'Stock was recently retrofitted to design.md. Do NOT redo the retrofit'). Dicatat, gak disentuh.
- Icon library beda (Phosphor `ph-*` di stok/dashboard/konversian vs Tabler `ti-*` di nav/crud-produk/export-gambar) -- pre-existing, resiko/effort gak sepadan tanpa browser test (alasan yang sama kayak yang udah dipakai buat kompres-pdf sesi lain).
- Beda 8px top-padding `main` dashboard (24px) vs stok (32px) -- dua-duanya valid di skala 8pt, keduanya SENGAJA di-scope pakai `:has()` selector (`#app-root:has(.stats-grid) main` vs `#app-root:has(.dropzone) main`) biar gak collide -- bukan bare global selector (salah satu risk class yang di-flag user buat dihindarin). Beda 8px-nya sendiri dianggap gak cukup kentara buat masuk P0/P1, bukan bagian dari 'shell' (cuma padding konten per-halaman).

**Verifikasi**: `node --check` (`.mjs`, ESM strict) di SEMUA 8 file yang disentuh sesi ini (`nav.js`, `nav-markup.js`, `crud-produk/index.js`, `crud-produk/markup.js`, `stok/markup.js`, `dashboard/markup.js`, `kompres-pdf/markup.js`, `export-gambar/markup.js`) -- semua lolos, zero syntax error. **BELUM ADA BROWSER TESTING SAMA SEKALI sesi ini** -- terutama fix bug #1 (nav search) BELUM dikonfirmasi beneran nutup bug yang dilaporin user, karena root cause-nya sendiri gak bisa dipastikan dari baca kode doang. Jangan anggap ketiga item ini 'selesai' sebelum user konfirmasi lewat browser asli, exactly sama kayak konvensi tiap sesi migrasi sebelumnya.


**Sesi kedua puluh lima -- follow-up user setelah browser-test sesi 24: search masih buggy di dark mode, minta hapus subtab konversian yang redundan, kompres-pdf "warnanya kerasa off, kayak patch job":**

**Nav search dark-mode bug, masih ada setelah fix sesi 24:** User kasih screenshot baru nunjukkin masih ada teks yang kelihatan redundan/dim di sekitar dropdown search pas dark mode. Fix sesi 24 (`.pw-nav--searching` class yang di-toggle, CSS `visibility:hidden` di `.pw-nav-eyebrow`/`.pw-nav-list`) GAK BISA dipastikan kenapa gagal cuma dari baca kode -- reasoning CSS cascade/stacking-nya udah dicek ulang dan SECARA SPEC harusnya jalan. Daripada nebak lagi, diganti pendekatannya total: bukan CSS class lagi, tapi `style.visibility` di-set LANGSUNG dari JS (`setSearchOverlayState()`, `nav.js`) ke elemen `.pw-nav-eyebrow`/`.pw-nav-list` lewat `navEl.querySelector(...)`. Ini ngilangin SEMUA kemungkinan masalah cascade/specificity yang gak bisa diverifikasi tanpa browser -- inline style SELALU menang, titik. CSS class lama (`.pw-nav--searching`) yang sekarang gak dipakai lagi dihapus dari `nav.css`, diganti komentar yang nunjuk ke `nav.js`.

**(Sambil investigasi) Ketemu 8-hasil di screenshot itu SEBENARNYA valid, bukan bug:** Ditelusuri `buildSearchIndex()` di `nav-markup.js` -- query "k" match ke module row "Conversion Workspace" (lewat kata 'workspace') + 6 subitem-nya (lewat sublabel "Conversion Workspace" yang sama) + module "Stok" (lewat 'k' di 'Stok') = PERSIS 8 hasil, pas sama `.slice(0,8)` cap yang ada di kode. Row pertama & terakhir yang keliatan "dim" di screenshot itu KEMUNGKINAN BESAR beda gara-gara list di baliknya emang belum ke-hide sempurna (bug yang mau difix), BUKAN gara-gara hasil itu sendiri salah -- gak bisa dipastikan mana yang bener tanpa render beneran, makanya fix di atas diambil (nutup semua kemungkinan, bukan 1 tebakan doang).

**Hapus subtab konversian yang redundan (user eksplisit minta):** `konversian/subnav.js`'s komentar dari sesi sebelumnya udah eksplisit nulis 'Removing the in-page row is the natural follow-up once this is confirmed working in a real browser' -- user sekarang minta itu. TAPI ditelusuri dulu index.js's `switchSubTab()` (baris 2478) -- manggil `.classList.toggle()` LANGSUNG ke `subtabCari`/`subtabSesi`/dst (const dari `getElementById`) TANPA null-check, dipanggil di BANYAK titik (mount, klik tab, dsb) -- kalau tombolnya dihapus dari markup, `getElementById` balikin `null`, dan `switchSubTab()` bakal THROW pas dipanggil pertama kali (mount()), MATIIN SELURUH HALAMAN. Jadi TIDAK dihapus dari markup.js -- sebagai gantinya, cuma disembunyikan via CSS (`pnm-universal.css`: `.subtab-btn{display:none}`, ditaruh SETELAH definisi asli `.subtab-btn{...display:flex...}` biar declaration belakangan menang buat property `display` doang). Elemen tetap ada di DOM, `switchSubTab()` tetap jalan normal (toggle class ke elemen yang sekarang invisible, gak crash), `.panel-collapse-btn` (tombol collapse panel search, SATU ROW sama subtab tapi class BEDA, fungsinya gak ada hubungannya sama nav) sengaja gak ikut disembunyiin.
**Trade-off yang keidentifikasi & didokumentasikan (BUKAN diselesaikan sesi ini):** badge count (`#sesi-badge`/`#setcari-badge`, nunjukkin jumlah item di clipboard konversi -- ini yang keliatan sebagai lingkaran merah '4' di screenshot user) HIDUP DI DALAM tombol yang sekarang disembunyikan, jadi SEKARANG GAK KELIATAN DI MANA PUN. Ditelusuri `sesiBadge`/`setcariBadge` punya 5 titik update tersebar (baris 2749-2750, 3321-3322, 6786, 6817, 6828-6829) -- portingnya ke elemen baru di sidebar (nav-markup.js's subitem) butuh nyentuh 5 titik itu plus nambah elemen baru, resiko lebih tinggi dari yang pas buat 'polish pass' tanpa browser. Diflag eksplisit ke user, bukan dikerjain diam-diam.

**Kompres PDF "warnanya kerasa off, kayak patch job":** Ditelusuri `.kp-page`'s token block (`:root`-nya sendiri, di-scope lokal, LIHAT komentar panjang di file itu dari sesi rewrite-nya) -- KETEMU akar masalahnya: waktu di-rewrite sesuai `design.md`, token-nya di-set PERSIS ke angka SPEK design.md (`--accent:#0070F3`, `--bg:#FAFAFA`, dst), BUKAN ke angka yang BENERAN dipakai `pnm-universal.css` (`--accent:#007AFF`, `--bg:#f0f2f5`, dst) -- dua angka itu MIRIP tapi BEDA (biru #0070F3 vs #007AFF, abu #FAFAFA vs #f0f2f5), which is PERSIS kenapa halaman ini kerasa 'sedikit beda warna' pas transisi dari/ke halaman lain, sama di light MAUPUN dark mode (dark juga beda: `--accent:#3291FF` vs pnm punya `#0A84FF`). Ini bukti design.md's SPEK sendiri emang belum direkonsiliasi ke pnm-universal.css yang BENERAN dipakai (persis catatan di design.md sendiri soal ini) -- kompres-pdf ngikutin dokumen spek, bukan implementasi nyata, jadi nyimpang dari SEMUA halaman lain yang justru ngikutin pnm-universal.css punya angka asli.
**Fix**: SEMUA nilai token di `.kp-page{}` (light) dan `html[data-theme="dark"] .kp-page{}` (dark) diganti ke NILAI ASLI `pnm-universal.css` (`--bg`,`--surface`,`--border`,`--accent`,`--danger`,`--warn`, dst), TAPI NAMA VARIABEL kompres-pdf sendiri (`--ink`,`--ink-soft`, dst -- BUKAN `--text`) SENGAJA GAK diganti, cuma NILAI-nya -- markup.js/index.js rujuk nama-nama itu di 25+ titik (per komentar file itu sendiri), ganti NAMA butuh nyentuh semua titik itu tanpa browser test, ganti NILAI di SATU blok token cukup, cascade otomatis nerusin ke semua pemakaian `var(--ink)` dst. Zero-risk, CSS-only, gak ada markup/JS yang disentuh. BONUS ketemu sambil di situ: `--success-dot`/`--warning-dot`/`--danger-dot` di blok DARK sebelumnya GAK PERNAH di-override (cuma didefinisiin di blok LIGHT) -- artinya dark mode kompres-pdf diam-diam masih makai warna dot LIGHT-mode selama ini (bug kecil terpisah, gak fatal tapi kurang kontras di background gelap) -- sekalian dibenerin, dikasih varian dark yang lebih terang (`--success-text`/`--warning-text`/`--danger-text` punya pnm-universal.css).

**Sidebar kosong di screenshot kompres-pdf user (image 3):** Ditelusuri `router.js` -- kompres-pdf gak ada exception/exclusion apa pun buat `navContainer` (logic `navMount()`/`display=''` sama persis kayak halaman lain). Gak ketemu alasan KODE yang bikin sidebar hilang spesifik di halaman ini. Kemungkinan besar itu screenshot ke-crop/discroll cuma nunjukkin area konten (breadcrumb 'Perkakas PDF' nempel di x=0 kayak gak ada sidebar di sebelah kirinya) -- TAPI ini CUMA DUGAAN, gak dikonfirmasi. Ditulis eksplisit ke user biar mereka yang konfirm ulang pas retest, bukan diasumsikan aman.

**Verifikasi**: `node --check` (`.mjs`) di `nav.js`. Brace-balance check di `pnm-universal.css` (1451/1451), `nav.css` (67/67), `kompres-pdf/style.css` (123/123) -- semua seimbang. **BELUM ADA BROWSER TESTING sesi ini juga** -- terutama fix search (masih strategi 'nutup semua kemungkinan', bukan 1 akar masalah yang udah dikonfirmasi), dan kompres-pdf's warna baru (harus dicek visual beneran, bukan cuma dicocokkan angka hex di kode).


**Sesi kedua puluh enam -- system-level unification pass, diminta eksplisit oleh user (bukan per-halaman lagi, level 'satu produk yang selesai'):** User minta baca design.md sebagai source of truth, jangan redesign/minimalis-in paksa, cuma converge yang udah ada. Prioritas dikerjain berdasar leverage (dampak ke berapa banyak halaman) dibagi resiko, bukan urutan yang diminta di brief.

**#1 leverage tertinggi -- nav.css's `.pw-nav`/`.pw-topbar` token block pakai angka SPEK design.md, bukan angka BENERAN `pnm-universal.css`:** Ini PERSIS kelas bug yang ketemu di kompres-pdf sesi 25 (`--accent:#0070F3` vs `pnm-universal.css` punya `#007AFF`, dst), TAPI di sini blast radius-nya jauh lebih gede -- `.pw-nav` (sidebar) render di SEMUA halaman tanpa kecuali, `.pw-topbar` (breadcrumb) di 4 dari 6 modul. Jadi drift warna yang di kompres-pdf cuma kena 1 halaman, di sini kena SELURUH APLIKASI sekaligus (sidebar selalu keliatan). Fix: value di `.pw-nav,.pw-topbar{...}` dan `[data-theme=dark] .pw-nav,.pw-topbar{...}` (nav.css) diganti ke angka `pnm-universal.css` yang beneran dipakai. Untuk token yang gak punya nama sama persis di `pnm-universal.css` (`--surface-raised`,`--success-dot`,`--warning-dot`,`--danger-dot`,`--fill-ghost-*`), dipetain ke token semantik terdekat: `--surface-raised` -> `pnm`'s `--surface-2` (biar ttp ada 'lift' visual dari `--surface` dasar, bukan disamain identik yang bakal ngilangin pembeda popover-vs-background), dot-colors -> `--success`/`--warning`/`--danger` (light) dan versi `-text` yang lebih terang (dark, buat kontras di background gelap), `--fill-ghost-selected`'s rgba dihitung ulang dari accent BARU (`rgb(0,122,255)` light / `rgb(10,132,255)` dark) biar tint-nya nyambung sama accent yang sekarang beneran dipake, bukan nyisa dari accent lama.

**#2 -- `.pw-topbar` ditambahin ke CRUD Produk** (satu-satunya modul yang SAMA SEKALI gak punya breadcrumb bar, penyebab paling keliatan dari 'app yang beda' -- 4 halaman lain: dashboard/stok/kompres-pdf/export-gambar semua udah punya). Ditelusuri dulu `.shell{display:flex;min-height:100vh}` + `.main{flex:1;...;display:flex;flex-direction:column}` (pnm-universal.css CRUD PRODUK section) -- gak ada height/calc(100vh) yang ngasumsiin jumlah row tertentu, jadi nambahin satu `<div class="pw-topbar">` sebagai child PERTAMA `.main` (sebelum `.topbar` internal yang udah ada) aman, cuma nambah 52px, konten di bawahnya (flex:1) otomatis nyesuain. Markup baru: `<div class="pw-topbar"><div class="pw-topbar-crumb"><strong>CRUD Produk</strong></div></div>`, gak ada actions (actions tetep di topbar internal yang lama). Konsisten sama pola yang UDAH established di kompres-pdf/export-gambar (breadcrumb bar shared DI ATAS toolbar/tab page-specific-nya sendiri) -- bukan pola baru.

**#3 -- Header brand duplikat konversian dihapus:** `.header-brand` (logo PNM + "Pionir Nusantara Manufacturing" + "Conversion Workspace") -- PERSIS anti-pattern yang disebut eksplisit di design.md's Top bar section ("Replaces the full brand header currently duplicated"). Ditelusuri dulu `grep 'header-brand\|logo-text\|logo-sub\|class="logo"'` di `konversian/index.js` -- NOL match, aman dihapus (bukan cuma di-hide kayak biasanya, karena beneran gak ada resiko JS). Diganti jadi `<div class="konv-crumb">Conversion Workspace</div>` di dalam `.header-brand` wrapper yang tetap dipertahanin (biar flex CSS-nya -- `display:flex;align-items:center;gap:10px;flex-shrink:0` -- gak perlu diubah). Style baru `.konv-crumb` (`konversian/style.css`) ditaruh gantiin `.logo-text`/`.logo-sub` rule yang sekarang gak kepake (dicek dulu: emang gak ada page lain yang masih pake `class="logo"`/`logo-text` di markup-nya -- pnm-universal.css punya rule buat itu jadi ATTIC/dead CSS sekarang, dibiarin, gak worth dibersihin sesi ini).

**#4 -- ketemu regresi nyata sambil nelusurin header-right (bukan yang dicari, ketemu gak sengaja):** `konversian/style.css` PUNYA rule dari sesi lama ('sesi keenam belas') yang HARUSNYA nyembunyiin link Beranda/Stok + tombol theme-toggle/logout (karena semua itu udah duplikat sama sidebar global) -- `#app-root .header-right a[href="/stok.html"]...{display:none}`. TAPI selector buat link Stok pake href LAMA (`/stok.html`), padahal markup.js's link SEKARANG pake `href="#stok"` (keganti pas migrasi hash-route di sesi lain, gak ada yang update selector hide-nya). Efeknya: link 'Stok' diam-diam JADI KELIATAN LAGI tanpa ada yang notice/sengaja -- literally 'obvious redundant navigation' yang user komplain, root cause-nya BUKAN belum pernah difix, tapi PERNAH difix terus REGRESI karena selector jadi stale. Fix: selector diganti ke `a[href="#stok"]`.
**#5 -- dicek juga link 'Kembali ke Beranda'**: href-nya `/app/shell.html#home` -- ditelusuri `router.js`, ternyata route `#home` UDAH DIRETIRE sesi keenam belas juga (`RETIRED_REDIRECTS={'home':'dashboard'}`, komentar di situ eksplisit bilang home page 'pure duplication' dari sidebar module list, makanya diretire). Jadi link ini SEBENARNYA udah gak fungsional-beda dari 'ke Dashboard' biasa. TAPI selector hide buat link INI (`a[href="/app/shell.html#home"]`) masih MATCH (href-nya emang gak pernah berubah, beda kasus dari Stok) -- jadi udah ke-hide dengan benar dari dulu, gak ada perubahan baru dibutuhin di sini, cuma dikonfirmasi statusnya pas lagi nelusurin yang lain.

**#6 -- spacing rhythm, top padding dashboard disamain ke stok/kompres-pdf:** dashboard's `main` padding top 24px vs stok/kompres-pdf 32px -- 32px yang menang (BUKAN dipilih sembarang, itu ANGKA EKSPLISIT di design.md's spacing scale: '32 — page top padding, gap between header and content'). Ketemu 2 rule TERPISAH yang nyetel nilai sama persis (`pnm-universal.css` baris ~320 di section DASHBOARD, DAN baris ~2098 di blok 'DASHBOARD size safeguards' belakangan di file yang sama) -- kalau cuma 1 yang diubah, yang laen (muncul belakangan di file, specificity sama) bakal MENANG dan nge-override balik ke 24px. DUA-duanya diubah bareng pake `sed` baris-spesifik (bukan str_replace teks biasa, karena isinya identik jadi ambiguous match).

**BELUM dikerjain, didokumentasikan sebagai leverage terbesar yang tersisa:**
- **Icon library split (Phosphor `ph-*` vs Tabler `ti-*`)** -- ini 'icon treatment' yang eksplisit diminta user buat di-unify, TAPI ditelusuri dulu risikonya: Phosphor dan Tabler gak ada mapping nama 1:1 (beda vendor, beda naming), swap butuh cek visual tiap glyph pengganti di puluhan titik (konversian's header-right yang tersisa, session-indicator, offline-banner, toggle-btn icons, dan kemungkinan tempat lain yang belum di-grep lengkap) TANPA browser access buat verifikasi tiap ikon kelihatan benar. Diputuskan TIDAK dieksekusi sesi ini -- resiko/effort gak sepadan buat 'polish pass' tanpa browser, beda kelas dari sekadar ganti NILAI token warna (yang cuma butuh 1 titik ubah, cascade otomatis). Diflag sebagai item PALING BESAR yang tersisa buat true 'satu bahasa visual'.
- Konversian's `<header>` MASIH elemen native sendiri, BUKAN literally `.pw-topbar` component -- brand duplikatnya udah ilang (item #3), tapi strukturnya (door-switch, session-indicator, dll semua dalam SATU row header custom) beda dari pola breadcrumb-bar-polos-di-atas-toolbar-sendiri yang dipake 5 halaman lain. Konsolidasi penuh butuh mikir ulang posisi elemen2 fungsional itu, di luar scope 'polish' sesi ini.
- Full typography audit (h1/h2/h3/body font-size tiap halaman dibanding skala 18/15/13.5/13px design.md) BELUM ditelusuri sistematis sesi ini -- cuma spacing top-padding yang kena.

**Verifikasi**: brace-balance check `pnm-universal.css` (1451/1451), `nav.css` (67/67), `konversian/style.css` (99/99) -- semua seimbang. `node --check` (`.mjs`) di `crud-produk/markup.js` dan `konversian/markup.js` -- lolos. **BELUM ADA BROWSER TESTING SAMA SEKALI sesi ini** -- yang PALING PENTING buat diverifikasi visual adalah item #1 (nav.css token change) karena dampaknya ke SEMUA halaman sekaligus; kalau ada yang keliatan aneh di sidebar/breadcrumb SETELAH ini, itu titik pertama yang harus dicek.
